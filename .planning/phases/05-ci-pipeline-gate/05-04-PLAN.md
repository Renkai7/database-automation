---
phase: 05-ci-pipeline-gate
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - scripts/ci/check-append-only.ts
  - tests/ci/check-append-only.test.ts
  - scripts/ci/check-schema-drift.ts
  - tests/ci/check-schema-drift.test.ts
  - packages/automation/src/adapter/drizzle-migrations.ts
  - packages/automation/src/index.ts
autonomous: true
requirements: [CI-05]

estimate:
  tokens: 27500
  raw_tokens: 55000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A pull request that modifies or deletes any existing file under `apps/recipe-app/drizzle/` fails a CI check; only additions are permitted."
    - "A pull request that changes an existing `_journal.json` entry's `idx`, `tag` or `when`, or removes an entry, fails a CI check — while appending a new entry passes."
    - "A pull request whose committed migration history no longer reconstructs `apps/recipe-app/src/db/schema.ts` fails a CI check, detected by whether generation produced a file, never by an exit code."
    - "The append-only check fails closed when the base or head commit cannot be resolved: a diff that could not be computed is never treated as an empty diff."
    - "The schema-drift check refuses to run when `git status --porcelain apps/recipe-app/drizzle/` is already non-empty before it invokes generation, so a dirty working tree can never be misread as 'no drift' and generated output can never be misread as a pre-existing edit."
    - "Every tamper-detection test drives synthetic before/after fixtures; no test mutates a real committed migration or the real journal."
    - "The journal entry shape has exactly one declaration in the repository: the CI check imports it type-only from the package barrel rather than declaring a second, silently-divergable copy."
  artifacts:
    - path: "scripts/ci/check-append-only.ts"
      provides: "D-09 sub-check 1 (file-level append-only) and D-10 (journal entry-level append-only)"
      exports: ["assertMigrationFilesAppendOnly", "assertJournalEntriesAppendOnly", "parseNameStatus"]
    - path: "scripts/ci/check-schema-drift.ts"
      provides: "D-09 sub-check 2 — the committed history still reconstructs schema.ts"
      exports: ["assertWorkingTreeClean", "driftSignalFromPorcelain"]
    - path: "tests/ci/check-append-only.test.ts"
      provides: "Synthetic-fixture proof of both append-only clauses, including the fail-closed unresolvable-diff case"
    - path: "tests/ci/check-schema-drift.test.ts"
      provides: "Synthetic-fixture proof of the drift signal and the dirty-tree refusal"
    - path: "packages/automation/src/index.ts"
      provides: "The journal shape as a type-only public export, so the CI check can share one definition instead of re-declaring it. Extends the existing `export type { MigrationFile } from \"./adapter/drizzle-migrations\"` statement rather than adding a second one."
      exports: ["Journal (type)", "JournalEntry (type)"]
  key_links:
    - from: "scripts/ci/check-append-only.ts"
      to: "packages/automation/src/index.ts"
      via: "type-only import of `Journal`/`JournalEntry` through the public barrel — one definition of what a journal entry is, and (because `import type` erases at compile time) no runtime coupling to the adapter's filesystem code"
      pattern: "drizzle-migrations"
    - from: "packages/automation/src/index.ts"
      to: "packages/automation/src/adapter/drizzle-migrations.ts"
      via: "the two shape declarations are currently unexported, so Task 2 adds the `export` keyword to both and widens the barrel's existing type-only re-export — the reuse above is made reachable by an in-scope edit, never assumed"
      pattern: "drizzle-migrations"
  prohibitions:
    - statement: "MUST NOT provide a named escape, label, or opt-out that lets a modified or deleted migration file through the append-only check — including a carve-out for reverts."
      status: resolved
      verification: judgment
---

<objective>
Build the two mechanical checks that catch a hand-edited migration and an edit to a migration a
previous merge already applied — without relying on a reviewer noticing.

Purpose: CI-05, via D-09 and D-10. The grounding fact established during discussion is that
`apps/recipe-app/drizzle/meta/_journal.json` carries **no content hash** — only `idx`, `version`,
`when`, `tag`, `breakpoints`; the sha256 lives solely in the `drizzle.__drizzle_migrations` table,
which this phase has no shared database to read. Git and `schema.ts` are therefore the only
available references, and every tamper design that assumed a hash is wrong.

Output: two `scripts/ci` checks with synthetic-fixture tests, ready for plan `05-06` to wire into
the `tamper-checks` job.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ci-pipeline-gate/05-CONTEXT.md
@.planning/phases/05-ci-pipeline-gate/05-RESEARCH.md
@.planning/phases/05-ci-pipeline-gate/05-PATTERNS.md
@packages/automation/src/adapter/drizzle-migrations.ts
@packages/automation/src/index.ts
@apps/recipe-app/drizzle/meta/_journal.json
@scripts/history-suite.ts
@docs/decisions.md
</context>

<!-- planner-discipline-allow: drizzle-kit -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: File-level append-only, failing closed on an unresolvable diff</name>
  <read_first>
    - `packages/automation/src/adapter/drizzle-migrations.ts` lines 79-127 — the
      hard-fail-never-warn error style **only** (an error names the exact field, the exact value
      observed, and why it cannot be trusted). This task parses path/status pairs, not journal
      JSON, so it imports nothing from this module: mirror the message style, that is all. Task 2
      is where the journal shape is actually shared, and Task 2 is what makes it exportable.
    - `scripts/history-suite.ts` (whole file) — the `execa(..., { reject: false })` +
      `import.meta.main` + `process.exitCode`-only convention, and its explicit "exit code is not
      enough" reasoning.
    - `apps/recipe-app/drizzle/meta/_journal.json` — the five real entries; the shape the
      comparator parses. Note it carries no hash field.
    - `.planning/phases/05-ci-pipeline-gate/05-RESEARCH.md` § Pitfall 3 — `actions/checkout`
      defaults to `fetch-depth: 1`, and a silently-empty diff looks identical to a genuinely-empty
      one. This is the reason the check must fail closed on an unresolvable commit.
    - `tests/history/tamper-then-refuse.test.ts` — the repository's own precedent for proving a
      tamper detector against synthetic before/after material rather than mutating shipped data.
  </read_first>
  <files>scripts/ci/check-append-only.ts, tests/ci/check-append-only.test.ts</files>
  <behavior>
    - `parseNameStatus` on `"A\tapps/recipe-app/drizzle/0005_new.sql"` yields one entry with status
      `A`.
    - `parseNameStatus` on a rename line (`R100\told\tnew`) yields an entry whose status is not `A`.
    - `assertMigrationFilesAppendOnly` on a diff containing only `A` statuses returns without
      throwing.
    - `assertMigrationFilesAppendOnly` on a diff containing `M apps/recipe-app/drizzle/0002_oval_maelstrom.sql`
      throws, and the message names that path.
    - `assertMigrationFilesAppendOnly` on a diff containing `D apps/recipe-app/drizzle/0003_backfill_steps_timer_label.sql`
      throws.
    - `assertMigrationFilesAppendOnly` ignores `apps/recipe-app/drizzle/meta/_journal.json`, which
      Task 2's entry-level check owns.
    - `assertMigrationFilesAppendOnly` on a `M` status for a `meta/*_snapshot.json` file throws —
      a snapshot is history, not a journal.
  </behavior>
  <action>
    Create `scripts/ci/check-append-only.ts`.

    Export `parseNameStatus(output: string): Array<{ status: string; path: string }>` — parses
    `git diff --name-status` output, tab-separated, tolerating rename/copy statuses that carry two
    path columns (take the destination path and keep the raw status letter with its similarity
    score intact, so `R100` is not silently normalised into `A`).

    Export `assertMigrationFilesAppendOnly(entries): void` — throws when any entry whose path is
    under `apps/recipe-app/drizzle/` and is not exactly
    `apps/recipe-app/drizzle/meta/_journal.json` has a status other than `A`. The thrown message
    must name the offending path and its observed status, and state that migrations are
    append-only — modelled on the journal-integrity error messages in
    `packages/automation/src/adapter/drizzle-migrations.ts`, which name the exact field and the
    exact value observed. No allowlist, no exemption, no named escape for reverts: D-10 rejected a
    revert carve-out on D-06's own grounds, and the plan records the consequence — Phase 4's D-32
    workflow (generate a BLOCKED migration, then revert both the file and its journal entry) would
    be refused if attempted through a pull request. That is the intended outcome, not a bug.

    Add the `import.meta.main` entry point. It reads `PR_BASE_SHA` and `PR_HEAD_SHA` from the
    environment — never `process.argv` — and, **before diffing**, verifies both are resolvable
    with `git cat-file -e <sha>^{commit}`. If either is missing from the environment or
    unresolvable in the local clone, throw with a message naming the missing/unresolvable SHA and
    stating that the workflow needs `fetch-depth: 0`. A diff that could not be computed must fail
    the check, never pass it: the warning sign Pitfall 3 names is a check that passes on every
    pull request including ones that genuinely modify a migration. Then run
    `git diff --name-status <base> <head> -- apps/recipe-app/drizzle/`, pass it through
    `parseNameStatus` and `assertMigrationFilesAppendOnly`, and set `process.exitCode` to 1 on a
    thrown error, 0 otherwise. Never call `process.exit()`.

    Write `tests/ci/check-append-only.test.ts` covering every row in the behavior block above,
    driving synthetic diff strings held in memory. No real git invocation, no temporary
    repository, no mutation of a shipped migration.
  </action>
  <acceptance_criteria>
    - `scripts/ci/check-append-only.ts` exports `parseNameStatus` and
      `assertMigrationFilesAppendOnly`, contains `import.meta.main`, and does not contain the
      string `process.exit(`.
    - The entry point resolves both SHAs before diffing: the source contains a `git cat-file`
      invocation.
    - Every row of the `<behavior>` block has a corresponding assertion in
      `tests/ci/check-append-only.test.ts`.
    - A test asserts that an unset `PR_BASE_SHA` produces a thrown error whose message contains
      the string `fetch-depth`.
    - `pnpm exec vitest run tests/ci/check-append-only.test.ts` passes with a non-zero test count.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/ci/check-append-only.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed" / "no test files found"</fails_when>
  </verify>
  <done>A modified or deleted migration file is refused, only additions pass, and a diff that could not be computed fails rather than passing silently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Journal entry-level append-only (D-10)</name>
  <read_first>
    - `scripts/ci/check-append-only.ts` — the module written in Task 1, which this extends.
    - `packages/automation/src/adapter/drizzle-migrations.ts` lines 23-35 — the `JournalEntry` and
      `Journal` declarations, the shape this task shares. **Read them as they actually are: as of
      this plan neither carries the `export` keyword, and neither name appears in
      `packages/automation/src/index.ts`.** Making them exportable is the first step of this
      task's action, not a precondition it may assume.
    - `packages/automation/src/adapter/drizzle-migrations.ts` lines 79-98 — the "ordering must be
      total" integrity error; mirror this message style.
    - `packages/automation/src/index.ts` line 5 —
      `export type { MigrationFile } from "./adapter/drizzle-migrations";`, the exact idiom this
      task widens, and line 1's header comment establishing the barrel as the package's only
      entry point (`03-CONTEXT.md` D-12). `tests/db-migrate-recover.test.ts` line 8 shows the
      matching consumer side already in use —
      `import type { RunnerClient } from "../packages/automation/src/index";` — a type-only import
      of a package type from outside the package.
    - `apps/recipe-app/drizzle/meta/_journal.json` — the five entries, their `idx`/`tag`/`when`
      fields, and the absence of any hash.
    - `.planning/phases/05-ci-pipeline-gate/05-CONTEXT.md` § D-10 — why `.sql` files are strictly
      append-only while the journal is append-only only at the entry level, and the recorded
      consequence for Phase 4's D-32 revert workflow.
  </read_first>
  <files>packages/automation/src/adapter/drizzle-migrations.ts, packages/automation/src/index.ts, scripts/ci/check-append-only.ts, tests/ci/check-append-only.test.ts</files>
  <behavior>
    - Base journal with entries idx 0..4, head journal identical plus a new idx 5: passes.
    - Head journal where idx 2's `tag` changed: throws, message names idx 2.
    - Head journal where idx 2's `when` changed: throws, message names idx 2.
    - Head journal where idx 3 is absent: throws, message names idx 3 and its base `tag`.
    - Head journal where entries were reordered but every existing entry's `idx`/`tag`/`when` is
      unchanged: passes — order in the array is not the identity, `idx` is.
    - Head journal that renumbers an existing entry's `idx` (closing the "renumber so a modified
      migration re-runs as new" dodge): throws.
    - Base journal text that is not valid JSON: throws, and the message says the base journal
      could not be parsed — never silently treated as "no entries".
  </behavior>
  <action>
    First, make the journal shape shareable — the reuse this plan claims is not currently
    reachable, so this step creates it rather than assuming it. In
    `packages/automation/src/adapter/drizzle-migrations.ts`, add the `export` keyword to the two
    shape declarations (`JournalEntry`, `Journal`). Visibility change only: do not add, rename or
    retype a field, and do not touch `enumerateMigrationFiles` or anything else in the module.
    Then widen the barrel's existing type-only re-export on `packages/automation/src/index.ts`
    line 5 so it carries `Journal` and `JournalEntry` alongside `MigrationFile` — one
    `export type { ... } from "./adapter/drizzle-migrations";` statement, not a second one.

    Why this rather than a local copy: the alternative is this script declaring its own matching
    shape, which is the second definition of a journal entry the plan's key_link exists to
    prevent — a field added to the real journal would then be reflected in one of the two and not
    the other, with nothing failing. Why type-only, and why in this direction: a script importing
    from the package is the permitted direction and already the established one
    (`scripts/db-migrate.ts` line 30), never the reverse, so `docs/decisions.md` D25's
    package-boundary inventory is untouched and gains no new entry; and `import type` erases at
    compile time, so `05-CONTEXT.md` D-17's no-`fetch`/no-`fs`/no-octokit rule is likewise
    untouched — the CI script gains a shape, not the adapter's filesystem code.

    Then extend `scripts/ci/check-append-only.ts` with
    `assertJournalEntriesAppendOnly(baseJournalText: string, headJournalText: string): void`,
    importing the shape as
    `import type { Journal, JournalEntry } from "../../packages/automation/src/index";` — through
    the barrel, never by reaching into the adapter module directly. Import only the names the
    comparator actually annotates with (`Journal` for a parsed file, `JournalEntry` for a single
    entry); the barrel exports both regardless, so a later need does not reopen this decision.

    Parse both texts. A parse failure on either side throws with a message naming which side
    failed — an unparseable base journal must never be treated as an empty entry list, because
    that would make every existing entry vacuously unchanged and turn the check into a no-op.

    Index the base entries by `idx`. For each base entry, find the head entry with the same `idx`;
    throw if it is absent, naming the `idx` and the base `tag`. Throw if the head entry's `tag` or
    `when` differs from the base entry's, naming the `idx` and which field changed. Do not compare
    array position — reordering is not tampering, and `idx` is the identity. Do not reject new
    entries with a higher `idx` — appending is the normal case that every new migration performs.
    Also throw when the same `idx` appears twice in the head journal, mirroring the
    "ordering must be total" integrity error the adapter already raises.

    Wire it into the `import.meta.main` entry point: obtain the base journal with
    `git show <base>:apps/recipe-app/drizzle/meta/_journal.json` and the head journal with
    `git show <head>:...` — read both from git object storage, not from the working tree, so the
    check is not influenced by anything the job did to the checkout. If `git show` fails for the
    base because the file did not exist at the base commit, treat that as "no prior journal" only
    when git's own error identifies a missing path, and say so in the log; any other failure
    throws.

    Extend `tests/ci/check-append-only.test.ts` with the behavior rows above, all synthetic JSON
    strings.
  </action>
  <acceptance_criteria>
    - `grep -c '^export interface JournalEntry' packages/automation/src/adapter/drizzle-migrations.ts`
      returns 1, and `grep -c '^export interface Journal {' packages/automation/src/adapter/drizzle-migrations.ts`
      returns 1 — both shapes are now exported.
    - `grep -n 'JournalEntry' packages/automation/src/index.ts` reports exactly one line, and that
      line begins with `export type {` — the barrel carries the shape type-only, inside the
      existing statement rather than a second one.
    - `grep -Ev '^[[:space:]]*//' scripts/ci/check-append-only.ts | grep -Ec 'interface (Journal|JournalEntry)'`
      returns 0 — the shape is imported, never re-declared locally. Comment lines are filtered
      first, so a header sentence describing the import cannot invalidate its own gate.
    - `scripts/ci/check-append-only.ts` exports `assertJournalEntriesAppendOnly`.
    - Every row of the `<behavior>` block has a corresponding assertion in
      `tests/ci/check-append-only.test.ts`.
    - A test asserts that an unparseable base journal throws rather than passing, and that the
      message distinguishes the base side from the head side.
    - A test asserts a reordered-but-unchanged head journal passes, proving the comparator keys on
      `idx` and not on array position.
    - `pnpm exec vitest run tests/ci/check-append-only.test.ts` passes.
    - `pnpm test` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/ci/check-append-only.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed"</fails_when>
    <automated>pnpm test</automated>
    <fails_when>non-zero exit, or the summary line reports any failed test file</fails_when>
  </verify>
  <done>Renumbering, retagging, re-timing or removing an already-applied journal entry is refused, while appending a new one passes — with no named escape — and the comparator reads the journal through the one exported shape the adapter already defines, not a second copy of it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Schema drift — the committed history still reconstructs schema.ts</name>
  <read_first>
    - `scripts/history-suite.ts` (whole file) — the exact child-process shape to copy, and its
      documented reason for never trusting an exit code alone.
    - `apps/recipe-app/drizzle.config.ts` — the module-load-time
      `assertLocalDevelopmentTarget(getDevDatabaseUrl())` call. This runs before generation ever
      does, which is why the drift job needs `RECIPE_DEV_DATABASE_URL` set to a pin-satisfying
      value even though it opens no socket (05-RESEARCH Pitfall 5).
    - `package.json` lines 6-25 — the existing script surface. `db:generate` is
      `pnpm --filter recipe-app exec drizzle-kit generate`; invoke that script name rather than the
      underlying binary, so this file stays clear of the sub-command bans
      `tests/guardrails.test.ts` enforces across the whole source surface.
    - `.planning/phases/05-ci-pipeline-gate/05-RESEARCH.md` § Pitfall 4 and § Assumptions Log A4 —
      generation's exit code does not distinguish clean from drift-found, and the drift-found case
      was never observed live. That is recorded as UNKNOWN, and the git working-tree diff is the
      verified signal.
    - `apps/recipe-app/drizzle/0003_backfill_steps_timer_label.sql` — the legitimately
      hand-authored Phase 4 backfill that generation would never produce. It is an `UPDATE`, so it
      yields no schema diff and this check correctly ignores it (D-09).
  </read_first>
  <files>scripts/ci/check-schema-drift.ts, tests/ci/check-schema-drift.test.ts</files>
  <behavior>
    - `driftSignalFromPorcelain("")` reports no drift.
    - `driftSignalFromPorcelain("?? apps/recipe-app/drizzle/0005_x.sql\n")` reports drift and names
      that path.
    - `driftSignalFromPorcelain(" M apps/recipe-app/drizzle/meta/0004_snapshot.json\n")` reports
      drift.
    - `assertWorkingTreeClean("")` returns without throwing.
    - `assertWorkingTreeClean(" M apps/recipe-app/drizzle/0002_oval_maelstrom.sql\n")` throws, and
      the message states that the tree was already dirty before generation ran.
  </behavior>
  <action>
    Create `scripts/ci/check-schema-drift.ts`.

    Export `assertWorkingTreeClean(porcelainOutput: string): void` — throws when the output is
    non-empty, with a message stating that the migrations directory was already modified before
    generation ran, so any subsequent result would be uninterpretable. This is the check's
    concurrency guarantee: a dirty tree can never be misread as "no drift", and generated output
    can never be mistaken for a pre-existing edit.

    Export `driftSignalFromPorcelain(porcelainOutput: string): { drifted: boolean; paths: string[] }`
    — parses `git status --porcelain` output, returning every reported path. Compare emptiness of
    output only; never compare against drizzle's randomly generated migration names or timestamps,
    which is what makes this deterministic (D-09).

    The `import.meta.main` entry point, in order: run
    `git status --porcelain -- apps/recipe-app/drizzle/` and pass it to `assertWorkingTreeClean`;
    run `execa("pnpm", ["db:generate"], { reject: false, stdio: "inherit" })`; run
    `git status --porcelain -- apps/recipe-app/drizzle/` again and pass it to
    `driftSignalFromPorcelain`; if `drifted` is true, print the paths and set `process.exitCode`
    to 1 with a message stating that `schema.ts` and the committed migration history no longer
    agree. Log the generation subprocess's exit code as an observation, and state in the log
    that it is recorded rather than relied on — assumption A4 is still UNKNOWN and this check
    deliberately does not depend on it. Never call `process.exit()`. Reads no `process.argv`.

    Write `tests/ci/check-schema-drift.test.ts` covering the behavior rows with synthetic
    porcelain strings only. Do not invoke generation from a test and do not create a temporary
    schema change: the real end-to-end falsification (deliberately hand-edit a migration, confirm
    the check fails) is performed once against a real pull request in plan `05-08`, where it can
    be observed rather than simulated.
  </action>
  <acceptance_criteria>
    - `scripts/ci/check-schema-drift.ts` exports `assertWorkingTreeClean` and
      `driftSignalFromPorcelain`, contains `import.meta.main`, and does not contain the string
      `process.exit(`.
    - The script invokes the `db:generate` package script, and `grep -c 'drizzle-kit' scripts/ci/check-schema-drift.ts`
      returns 0 (the underlying binary is never named in this file).
    - Every row of the `<behavior>` block has a corresponding assertion in
      `tests/ci/check-schema-drift.test.ts`.
    - `pnpm exec vitest run tests/ci/check-schema-drift.test.ts` passes.
    - `pnpm exec vitest run tests/guardrails.test.ts` passes — the two new scripts are inside the
      guardrail suite's source surface.
    - `git status --porcelain apps/recipe-app/drizzle/` is empty after the task, proving no
      generation side effect was committed.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/ci/check-schema-drift.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed" / "no test files found"</fails_when>
    <automated>pnpm exec vitest run tests/guardrails.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed"</fails_when>
    <automated>pnpm exec tsx scripts/ci/check-schema-drift.ts</automated>
    <fails_when>non-zero exit against the unchanged committed history — the current schema and history agree, so a failure here means the check reports drift where there is none</fails_when>
  </verify>
  <done>Running the drift check against the unchanged repository reports no drift, a dirty tree is refused rather than interpreted, and the signal is the appearance of a file rather than an exit code the researcher recorded as UNKNOWN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pull-request branch → tamper checks | The author controls both the migration files and the journal being compared. |
| git object storage → check input | The base journal is read from git, not from the working tree the job may have modified. |
| working tree → drift signal | Generation writes into the same directory the check inspects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-19 | Tampering | editing a migration a previous merge already applied | critical | mitigate | `assertMigrationFilesAppendOnly` refuses any non-added path under the migrations directory, with git as the reference and no exemption list. |
| T-05-20 | Tampering | renumbering or deleting a journal entry so a modified migration re-runs as new | high | mitigate | `assertJournalEntriesAppendOnly` keys on `idx` and compares `tag` and `when`; a disappearing entry and a duplicated `idx` both throw. |
| T-05-21 | Tampering | a shallow clone producing an empty diff that looks like a clean pull request | critical | mitigate | Both SHAs are resolved with `git cat-file` before diffing; an unresolvable SHA throws and names `fetch-depth`. Plan `05-06` sets `fetch-depth: 0` on the job. |
| T-05-22 | Tampering | an unparseable base journal read as "no prior entries", making the comparison vacuous | high | mitigate | A parse failure on either side throws and names which side failed. |
| T-05-23 | Spoofing | a dirty working tree misread as "no drift", or generated output misread as a pre-existing edit | high | mitigate | `assertWorkingTreeClean` runs before generation and refuses a non-empty porcelain result. |
| T-05-24 | Repudiation | relying on a generation exit code whose drift-found behaviour was never observed | medium | mitigate | The exit code is logged as an observation and never used as the signal; assumption A4 stays UNKNOWN in the record. |
| T-05-25 | Tampering | a detector proven by mutating real shipped migration data | medium | mitigate | Every test drives synthetic in-memory fixtures, following the repository's own WR-04 precedent; the real falsification is performed once, observably, in plan `05-08`. |
</threat_model>

<verification>
- `pnpm exec vitest run tests/ci/check-append-only.test.ts tests/ci/check-schema-drift.test.ts`
  passes.
- `pnpm exec tsx scripts/ci/check-schema-drift.ts` exits 0 against the unchanged repository.
- `pnpm test` passes — including `tests/guardrails.test.ts`, whose D25 package-boundary inventory
  must still match exactly: this plan adds no import to `packages/automation`, only two `export`
  keywords and one widened type-only re-export, so the inventory is expected to be untouched.
- `git status --porcelain apps/recipe-app/drizzle/` is empty.
</verification>

<success_criteria>
Both clauses of criterion 3 have a mechanical check: an edit to an already-applied migration is
caught by git, and a hand-edit that changed what the SQL does is caught by whether generation
produces a file. Neither depends on a reviewer noticing, and neither can pass by failing to run.
</success_criteria>

<artifacts_this_phase_produces>
Introduced by this plan:

- **New CI scripts:** `scripts/ci/check-append-only.ts` exporting `parseNameStatus`,
  `assertMigrationFilesAppendOnly`, `assertJournalEntriesAppendOnly`; and
  `scripts/ci/check-schema-drift.ts` exporting `assertWorkingTreeClean`,
  `driftSignalFromPorcelain`.
- **New tests:** `tests/ci/check-append-only.test.ts`, `tests/ci/check-schema-drift.test.ts`.
- **Widened package type surface:** `Journal` and `JournalEntry` become exported types on
  `packages/automation/src/adapter/drizzle-migrations.ts` and are re-exported type-only from the
  barrel, so the append-only comparator shares the adapter's one definition of a journal entry.
  Type-only and outbound (script imports package), so neither `docs/decisions.md` D25's boundary
  inventory nor D-17's purity rule is affected.
- **Environment contract extended:** both scripts read `PR_BASE_SHA` / `PR_HEAD_SHA` from the
  environment; neither reads `process.argv`. The drift check additionally requires
  `RECIPE_DEV_DATABASE_URL` to be set to a pin-satisfying value at job level even though it opens
  no socket.
- **New required-check name:** `tamper-checks` is wired in plan `05-06`; these scripts are what it
  runs.

Phase-wide published contract (under D-04 the required-check names become ruleset configuration,
so they are a contract, not an internal detail):

- **Required-check names / workflow job `name:` values:** `analyze`, `tamper-checks`, `test`,
  `test-history`, `migrate`, `ruleset-config-check`.
- **New workflow files:** `.github/workflows/pr-gate.yml`, `.github/workflows/restore-drill.yml`.
- **New ruleset payload:** `.github/rulesets/main-protection.json` (ruleset name `main-protection`).
- **New package exports:** `renderPrComment`, `renderAnalyzerFailureComment`, `PR_COMMENT_MARKER`,
  type `AnalyzedFile`; plus types `Journal` and `JournalEntry`, promoted from unexported
  declarations in `adapter/drizzle-migrations.ts` and re-exported type-only from the barrel by
  plan `05-04` so the append-only comparator shares one definition of a journal entry.
- **New CI scripts (all):** `audit-history.ts`, `analyze-gate.ts`, `check-append-only.ts`,
  `check-schema-drift.ts`, `apply-ruleset.ts`, `check-ruleset-config.ts` — all under `scripts/ci/`.
- **New docs:** `docs/40-public-release-audit.md`, `docs/40-ci-gate-merge-attempt.md`.
- **New pnpm scripts:** none — CI-internal scripts are invoked as
  `pnpm exec tsx scripts/ci/<name>.ts`.
</artifacts_this_phase_produces>

<flagged_assumptions>
Surfaced, not silently dropped (spec-less probe fallback, §A):

- **CI-02 (unclassified edge probe)** — no shape category returned; covered by plan `05-03`'s
  exit-code mapping and its per-code tests.
- **CI-06 (unclassified edge probe)** — no shape category returned; covered by plans `05-06` and
  `05-07`.

Carried from `05-RESEARCH.md` and deliberately left UNKNOWN by this plan rather than assumed:
assumption A4 — whether generation exits non-zero when it *does* produce a migration. This check
does not depend on the answer; the git working-tree signal works either way, which is why the
unknown is recorded rather than resolved by guessing.
</flagged_assumptions>

<output>
Create `.planning/phases/05-ci-pipeline-gate/05-04-SUMMARY.md` when done.
</output>
