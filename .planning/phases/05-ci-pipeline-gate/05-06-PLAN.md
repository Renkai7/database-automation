---
phase: 05-ci-pipeline-gate
plan: 06
type: execute
wave: 5
depends_on: ["05-04", "05-05"]
files_modified:
  - .github/workflows/pr-gate.yml
  - .github/workflows/restore-drill.yml
autonomous: true
requirements: [CI-01, CI-05, CI-06]

estimate:
  tokens: 27500
  raw_tokens: 55000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Every pull request runs the analyzer, both migration history test axes (empty-database and existing-database), and the application test suite — each as its own named check."
    - "No job in `.github/workflows/pr-gate.yml` declares `needs:` on another job in the same workflow: the six required checks are order-independent, and the merge decision depends only on all six reporting success rather than on which reported first."
    - "Inspecting the workflow shows a job whose entire body is the migration run — invoked by the pipeline, structurally separate from any job that builds or boots the application."
    - "The migrate job's database is reachable at `127.0.0.1:5432/recipe_dev`, so `assertLocalDevelopmentTarget`'s pin is satisfied with no loosening and the runner's thin entry point runs unmodified."
    - "No required job carries a conditional skip and the workflow carries no path filter — a check that reports success without running is a second door."
    - "The restore drill runs on a schedule and is not a required per-pull-request check; when it fails it reports somewhere the owner will see, because it has no pull request to comment on."
    - "Every job runs on `ubuntu-latest`; a Windows-only regression will surface on the development machine rather than in CI, and that is recorded rather than papered over."
  artifacts:
    - path: ".github/workflows/pr-gate.yml"
      provides: "All six required checks: analyze, tamper-checks, test, test-history, migrate, ruleset-config-check"
      contains: "name: migrate"
    - path: ".github/workflows/restore-drill.yml"
      provides: "The scheduled restore drill (D-13), deliberately not a required check"
      contains: "schedule"
  key_links:
    - from: ".github/workflows/pr-gate.yml"
      to: "scripts/ci/check-append-only.ts"
      via: "the tamper-checks job invokes it with PR_BASE_SHA/PR_HEAD_SHA under fetch-depth: 0"
      pattern: "check-append-only"
    - from: ".github/workflows/pr-gate.yml"
      to: "scripts/db-migrate.ts"
      via: "the migrate job runs the real pnpm db:migrate entry point unmodified against a postgres:17 service container"
      pattern: "db:migrate"
    - from: ".github/workflows/pr-gate.yml"
      to: "scripts/ci/check-ruleset-config.ts"
      via: "the ruleset-config-check job invokes it, and is the only job granted administration: write"
      pattern: "check-ruleset-config"
  prohibitions:
    - statement: "MUST NOT give CI any database credential or connection beyond an ephemeral, in-job container; no staging or production target exists in this phase, and no repository secret beyond the default workflow token is added."
      status: resolved
      verification: judgment
    - statement: "MUST NOT make any required job conditionally skippable — a check that reports success without running is a second door, which D-04 already declined once for documentation-only changes."
      status: resolved
      verification: judgment
---

<objective>
Expand the proven `analyze` slice into the full set of six required checks, and move the restore
drill onto a schedule.

Purpose: CI-01 (the analyzer, both migration history test axes, and the application tests all run
on every pull request), CI-05 (the tamper checks built in plan `05-04` become a real gate), and
CI-06 (a dedicated migrate job that a reader of the workflow file identifies as "migrations run as
their own isolated pipeline step"). D-13 puts `pnpm test:drill` on a schedule instead, so
`02-CONTEXT.md` D-19's 30-day staleness gate stops depending on the owner remembering to run it
locally — this project's own "architectural enforcement over remembered caution" applied to the one
requirement the brief called its single confirmed operational risk.

Output: `pr-gate.yml` carrying all six named jobs, and a scheduled `restore-drill.yml`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ci-pipeline-gate/05-CONTEXT.md
@.planning/phases/05-ci-pipeline-gate/05-RESEARCH.md
@.planning/phases/05-ci-pipeline-gate/05-03-SUMMARY.md
@docker-compose.yml
@package.json
@scripts/db-migrate.ts
@scripts/env.ts
@docs/decisions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The four remaining pull-request jobs — tamper-checks, test, test-history, ruleset-config-check</name>
  <read_first>
    - `.github/workflows/pr-gate.yml` — the workflow written in plan `05-03`: its trigger, its
      workflow-level `permissions`, its `concurrency` block, and the `analyze` job's exact step
      sequence, which the new jobs mirror.
    - `.planning/phases/05-ci-pipeline-gate/05-03-SUMMARY.md` — the verbatim check name GitHub
      actually reported for `analyze` on a real run. Every job added here uses the same `name:`
      convention that produced it, because those six strings are already committed in
      `.github/rulesets/main-protection.json`.
    - `package.json` lines 6-25 — the exact script names CI invokes: `db:analyze:migrations`,
      `db:migrate`, `db:seed`, `test`, `test:history`, `test:drill`. Confirm each name against
      this file before writing it into a step.
    - `docker-compose.yml` lines 11-30 — the `postgres:17` pin, `POSTGRES_USER: recipe_app`,
      `POSTGRES_DB: recipe_dev`, the loopback-only `127.0.0.1:5432:5432` publication, and the
      `pg_isready -U recipe_app -d recipe_dev` health check. The CI service container mirrors it.
    - `scripts/env.ts` lines 55-70 — `DEV_DATABASE_HOST_ALLOWLIST`, `EXPECTED_DEV_DATABASE_PORT`
      (`5432`), `EXPECTED_DEV_DATABASE_NAME` (`recipe_dev`), `EXPECTED_DEV_DATABASE_ROLE`
      (`recipe_app`). CI is shaped to satisfy these; they are never loosened to fit CI.
    - `apps/recipe-app/drizzle.config.ts` lines 14-16 — the module-load-time target assertion that
      makes the connection variable mandatory even for a job that opens no socket
      (05-RESEARCH Pitfall 5).
    - `vitest.config.ts` and `scripts/history-suite.ts` — what `pnpm test` and `pnpm test:history`
      each actually run, and that the history suite starts its own Testcontainers postgres rather
      than consuming a service container.
    - `.planning/phases/05-ci-pipeline-gate/05-RESEARCH.md` § Postgres service container example
      and § Pitfall 3.
  </read_first>
  <files>.github/workflows/pr-gate.yml</files>
  <action>
    Add four jobs to `.github/workflows/pr-gate.yml`. Each job's id and `name:` are identical, and
    no job declares `needs:` — the six checks are independent, and the merge decision depends on
    all six reporting success rather than on any ordering between them.

    Every job repeats the preamble the `analyze` job established: `actions/checkout@v4` with
    `ref: ${{ github.event.pull_request.head.sha }}`, `pnpm/action-setup@v4`,
    `actions/setup-node@v4` (node 24, `cache: pnpm`), `pnpm install --frozen-lockfile`. Duplicate
    that preamble rather than extracting a composite action: six explicit job bodies are what makes
    criterion 4 inspectable, and a shared action would hide the structure a reader is meant to see.
    `runs-on: ubuntu-latest` on every job — D-12, because GitHub's Windows runners can only run
    Windows containers, so `postgres:17` and Testcontainers cannot work there at any price.

    **`tamper-checks`** — no database. Set `fetch-depth: 0` on the checkout: the default of 1 makes
    the base commit unresolvable, and a diff that could not be computed looks exactly like a clean
    one. Two steps: `pnpm exec tsx scripts/ci/check-append-only.ts` with
    `PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}` and
    `PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}` in its `env:`; then
    `pnpm exec tsx scripts/ci/check-schema-drift.ts` with the shared connection variable set. The
    drift check needs that variable even though it opens no socket, because
    `apps/recipe-app/drizzle.config.ts` asserts the target at module-load time before generation
    ever runs — a job that omits it fails with a message about a missing connection variable and
    says nothing at all about schema drift.

    **`test`** — a `postgres:17` service container with `POSTGRES_USER: recipe_app`,
    `POSTGRES_DB: recipe_dev`, the throwaway password described below, `ports` published as the
    loopback-only form `127.0.0.1:5432:5432` (mirroring `docker-compose.yml`'s own convention
    rather than the bare shorthand most examples show), and health-check options mirroring the
    compose file's `pg_isready -U recipe_app -d recipe_dev`. Steps in order: `pnpm db:migrate`,
    `pnpm db:seed`, `pnpm test`. Add a YAML comment recording that `pnpm test` includes
    `tests/smoke.test.ts`, which performs a real production build and boots the application — which
    is exactly why D-14 requires the migrate job in Task 2 to be a separate job from this one. The
    migrate step here is test-fixture setup invoked explicitly by the pipeline; it is not the CI-06
    demonstration and it is never an application startup hook.

    **`test-history`** — Docker is available on the runner and the suite starts its own ephemeral
    `postgres:17` containers, so this job has no `services:` block. One step: `pnpm test:history`,
    which runs both axes unmodified from Phase 4. Give it a generous `timeout-minutes` because
    Testcontainers pays a first-run image pull, and record the observed wall-clock duration in the
    SUMMARY — `05-VALIDATION.md` lists feedback latency as UNKNOWN, and the first real run is where
    it stops being unknown.

    **`ruleset-config-check`** — no database. `permissions: administration: write` on this job and
    this job only; the workflow-level default stays `contents: read`. One step:
    `pnpm exec tsx scripts/ci/check-ruleset-config.ts` with `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}`.
    Add a YAML comment stating the honest limit: this check runs inside the thing it audits, so it
    raises the cost of tampering and makes it visible, and it does not make it impossible.

    Set the shared connection variable at job level for the jobs that need it, to a value that
    satisfies the pin exactly: user `recipe_app`, the throwaway password, host `127.0.0.1`, port
    `5432`, database `recipe_dev`. That password is an ephemeral per-job container credential
    guarding nothing that outlives the job; name it so this is obvious in the file itself (for
    example `ci_ephemeral_not_a_secret`), and record in the SUMMARY that it was reviewed and
    deliberately committed as a non-secret. Do not add a repository secret: `05-CONTEXT.md`'s
    domain boundary allows no credentials in CI beyond the default workflow token, and there is no
    database secret to add yet.

    Add no conditional-skip expression to any of these four jobs and no path filter to the
    workflow trigger. A conditionally-skipped required job is exactly the skip-with-success shape
    D-04 declined for documentation-only changes. The recorded consequence stands: every
    `.planning/` documentation commit now travels through a branch and a pull request too.
  </action>
  <acceptance_criteria>
    - With comment lines stripped, `.github/workflows/pr-gate.yml` contains exactly one line each
      of `name: analyze`, `name: tamper-checks`, `name: test-history`, `name: ruleset-config-check`.
    - The stripped file contains zero occurrences of `needs:`.
    - The stripped file contains `administration: write` exactly once.
    - The stripped file contains `127.0.0.1:5432:5432` and contains no bare `- 5432:5432` line.
    - The stripped file contains `runs-on: ubuntu-latest` and no other runner label.
    - Every `run:` step names a script that exists: each `pnpm <name>` invocation matches a key in
      `package.json`'s `scripts` block, and each `pnpm exec tsx scripts/ci/<file>.ts` names a file
      present in `git ls-files`.
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^[[:space:]]*#' .github/workflows/pr-gate.yml | grep -c 'needs:'</automated>
    <fails_when>the printed count is anything other than 0</fails_when>
    <automated>grep -v '^[[:space:]]*#' .github/workflows/pr-gate.yml | grep -c 'administration: write'</automated>
    <fails_when>the printed count is anything other than 1</fails_when>
    <automated>grep -v '^[[:space:]]*#' .github/workflows/pr-gate.yml | grep -c '127.0.0.1:5432:5432'</automated>
    <fails_when>the printed count is 0</fails_when>
    <automated>for n in analyze tamper-checks test-history ruleset-config-check; do c=$(grep -v '^[[:space:]]*#' .github/workflows/pr-gate.yml | grep -c "name: $n$"); echo "$n=$c"; done</automated>
    <fails_when>any printed pair is not exactly `=1`</fails_when>
  </verify>
  <done>Five jobs exist and are independent: analyze, tamper-checks, test, test-history and ruleset-config-check, each named exactly as the committed ruleset payload expects, none conditional, none ordered.</done>
</task>

<task type="auto">
  <name>Task 2: The dedicated migrate job — CI-06's inspectable artifact</name>
  <read_first>
    - `scripts/db-migrate.ts` (whole file, 131 lines) — the entry point this job runs unmodified.
      Note that it calls `getDevDatabaseUrl()` then `assertLocalDevelopmentTarget(url)` then
      `assertDevelopmentDatabase(client)` (which compares `current_database()` against
      `recipe_dev`) before anything else, and that it accepts no target of any kind.
    - `scripts/env.ts` — `assertLocalDevelopmentTarget`'s three pinned components and
      `assertDevelopmentDatabase`'s `current_database()` check. The CI database is shaped to
      satisfy all four; none of them is weakened.
    - `packages/automation/src/runner/exit-codes.ts` — `RUNNER_EXIT_CODES`. Its own header names
      this phase as the reader of the contract: `APPLIED` is 0, and `1` is deliberately not a
      member of the set, so an uncaught crash can never be misread as a named outcome.
    - `.planning/phases/05-ci-pipeline-gate/05-CONTEXT.md` § D-14 — why the history suite implying
      a migration run is not enough, and why a step named after a test suite does not read as
      "migrations run as their own isolated pipeline step".
    - `.github/workflows/pr-gate.yml` — the `test` job written in Task 1, whose service-container
      block this one mirrors.
  </read_first>
  <files>.github/workflows/pr-gate.yml</files>
  <action>
    Add one more job, id and `name:` both `migrate`. Its entire body, after the shared preamble, is
    a single step running `pnpm db:migrate`. Nothing else. No build, no seed, no test, no
    application boot — that structural emptiness is the deliverable, because criterion 4 is a claim
    about what a reader sees when inspecting the workflow.

    Give it the same `postgres:17` service container as the `test` job: `POSTGRES_USER: recipe_app`,
    `POSTGRES_DB: recipe_dev`, the same throwaway password, `127.0.0.1:5432:5432`, and the
    `pg_isready` health check. This is the load-bearing detail from D-14: with the database
    reachable at `127.0.0.1:5432/recipe_dev`, `assertLocalDevelopmentTarget`'s pin on host, port
    and database name is satisfied honestly, `assertDevelopmentDatabase`'s `current_database()`
    comparison passes, and `scripts/db-migrate.ts` runs completely unmodified. Shape the CI
    database to fit the pin; never loosen the pin to fit CI.

    Add a YAML comment above the job stating in one sentence what a reader is meant to conclude
    from it: migrations run as their own pipeline step, invoked by the pipeline, and nothing in the
    application's own startup path triggers them (`docs/decisions.md` D8). Point the comment at
    `tests/guardrails.test.ts` as the place the second half of that claim is mechanically enforced
    — plan `05-07` adds it.

    Record in the SUMMARY the exit code `pnpm db:migrate` returned on the real run and match it
    against `RUNNER_EXIT_CODES.APPLIED`. A run that exits with something outside that named set is
    a crash, not an outcome.
  </action>
  <acceptance_criteria>
    - The stripped workflow contains exactly one `name: migrate` line.
    - The `migrate` job's `steps:` block contains exactly one `run:` step beyond the shared
      preamble, and that step's command is `pnpm db:migrate`.
    - The `migrate` job contains no step invoking a build, a seed, a test runner, or the
      application.
    - The `migrate` job declares a `services:` block with image `postgres:17` and
      `POSTGRES_DB: recipe_dev`.
    - `scripts/db-migrate.ts` is unchanged by this plan: `git diff --stat HEAD -- scripts/db-migrate.ts`
      is empty.
    - `scripts/env.ts` is unchanged by this plan.
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^[[:space:]]*#' .github/workflows/pr-gate.yml | grep -c 'name: migrate$'</automated>
    <fails_when>the printed count is anything other than 1</fails_when>
    <automated>grep -v '^[[:space:]]*#' .github/workflows/pr-gate.yml | grep -c 'pnpm db:migrate'</automated>
    <fails_when>the printed count is 0 (the migrate job does not invoke the real entry point)</fails_when>
    <automated>git diff --stat HEAD -- scripts/db-migrate.ts scripts/env.ts</automated>
    <fails_when>any output at all — the runner entry point and the environment pin must be untouched by this plan</fails_when>
  </verify>
  <done>A reader opening the workflow file sees a job called `migrate` whose whole body is the real migration command against its own database, separate from every job that builds or boots the app, with the pin satisfied rather than loosened.</done>
</task>

<task type="auto">
  <name>Task 3: The scheduled restore drill</name>
  <read_first>
    - `scripts/drill.ts` — what `pnpm test:drill` ultimately exercises, its "the drill never ran is
      not the drill failed" boundary, and its status-record behaviour.
    - `docs/restore-drill-status.json` — the committed staleness input `02-CONTEXT.md` D-19's
      30-day gate reads, and which this schedule is intended to keep fresh.
    - `vitest.drill.config.ts` and `package.json`'s `test:drill` script — the exact invocation.
    - `.planning/phases/05-ci-pipeline-gate/05-CONTEXT.md` § D-13 — why the drill is scheduled
      rather than required per pull request, and `02-CONTEXT.md` D-16's reasoning that a slow
      Docker-dependent suite in the fast path is the one that eventually gets marked skipped.
    - `.github/workflows/pr-gate.yml` — the preamble to mirror.
  </read_first>
  <files>.github/workflows/restore-drill.yml</files>
  <action>
    Create `.github/workflows/restore-drill.yml`. Trigger on `schedule` with a weekly cron, plus
    `workflow_dispatch` so the owner can run it on demand without waiting for the cadence. Weekly
    is chosen against `02-CONTEXT.md` D-19's 30-day staleness gate: four runs inside every staleness
    window means three consecutive failures can occur before the gate would have fired anyway.
    Record that reasoning in a YAML comment so the cadence is a decision, not a default.

    `runs-on: ubuntu-latest`, workflow-level `permissions: contents: read` with `issues: write` on
    the job, and the same checkout/pnpm/node preamble. One step: `pnpm test:drill`.

    Because this workflow has no pull request to comment on, a failure must surface somewhere the
    owner sees. Add a final step conditioned on the job having failed that opens or updates a
    GitHub issue via `gh issue create` / `gh issue comment`, titled so repeated failures land on
    one issue rather than accumulating. Search for an existing open issue with that exact title
    first (`gh issue list --search`), and comment on it if found. This is the one place in the phase
    where a conditional step is correct: it is not a required check and the condition is
    "something already failed", not "skip the check".

    This workflow is deliberately **not** in the ruleset's required-status-check list. State that
    in a YAML comment with the reason, so a future reader does not add it "for completeness" and
    put a slow Docker suite back in the merge path.

    Trigger one run with `gh workflow run` and record the observed outcome and duration in the
    SUMMARY. A scheduled workflow that has never run once is an assumption, not a mechanism.
  </action>
  <acceptance_criteria>
    - `.github/workflows/restore-drill.yml` exists and, with comments stripped, contains
      `schedule:`, `workflow_dispatch:`, and `pnpm test:drill`.
    - The file contains a comment stating that it is deliberately not a required check and why.
    - `.github/rulesets/main-protection.json` contains no context naming the drill.
    - `gh workflow list` includes the restore-drill workflow, and `gh run list --workflow restore-drill.yml`
      shows at least one completed run whose conclusion is recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^[[:space:]]*#' .github/workflows/restore-drill.yml | grep -c 'pnpm test:drill'</automated>
    <fails_when>the printed count is 0</fails_when>
    <automated>grep -v '^[[:space:]]*#' .github/workflows/restore-drill.yml | grep -cE 'schedule:|workflow_dispatch:'</automated>
    <fails_when>the printed count is less than 2</fails_when>
    <automated>node -e "const r=require('./.github/rulesets/main-protection.json');const c=r.rules.find(x=>x.type==='required_status_checks').parameters.required_status_checks.map(x=>x.context);if(c.some(x=>x.includes('drill'))){console.error('drill is a required check: '+c.join(','));process.exit(1);}console.log('drill is not a required check');"</automated>
    <fails_when>non-zero exit, or stderr contains "drill is a required check"</fails_when>
    <automated>gh run list --workflow restore-drill.yml --limit 1 --json conclusion --jq '.[0].conclusion'</automated>
    <fails_when>non-zero exit, empty output (the workflow has never run), or a printed conclusion other than `success`</fails_when>
  </verify>
  <done>The restore drill runs on a weekly schedule and on demand, has actually run once, reports a failure onto a single tracking issue, and is deliberately absent from the required-check list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pull-request branch content → six CI jobs | Author-controlled code and SQL execute on a runner holding a workflow token. |
| workflow token → GitHub repository configuration | One job is granted `administration: write`; the rest are not. |
| CI job → ephemeral postgres service container | The only database this phase touches; it exists for the duration of one job. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-34 | Elevation of Privilege | over-privileged workflow token inherited by every job | high | mitigate | Workflow-level `permissions: contents: read`; `pull-requests: write` only on `analyze`; `administration: write` only on `ruleset-config-check`; `issues: write` only in the drill workflow. Asserted by the exactly-once check on `administration: write`. |
| T-05-35 | Tampering | a required job made conditionally skippable, reporting success without running | critical | mitigate | No conditional-skip expression on any pull-request job and no path filter on the trigger; the workflow is asserted to contain zero `needs:` and the job set is asserted by name. |
| T-05-36 | Tampering | a migration path that runs at application startup rather than as a pipeline step | high | mitigate | The dedicated `migrate` job is the pipeline-invoked path, asserted to contain exactly one command; plan `05-07` adds the mechanical guardrail that the application's own source never triggers a migration at boot. |
| T-05-37 | Elevation of Privilege | loosening the development-target pin to make CI work | critical | mitigate | The service container is shaped to satisfy the pin (`127.0.0.1:5432/recipe_dev`, role `recipe_app`); `scripts/env.ts` and `scripts/db-migrate.ts` are asserted unchanged by `git diff --stat`. |
| T-05-38 | Information Disclosure | a committed database password in a public repository | medium | accept | The value is an ephemeral per-job container credential that guards nothing outliving the job, named in the file so that is self-evident, and reviewed and recorded in the SUMMARY. No repository secret is added, per `05-CONTEXT.md`'s domain boundary. |
| T-05-39 | Denial of Service | a slow Docker suite in the merge path getting marked skipped over time | medium | mitigate | The drill is scheduled, not required (D-13 / `02-CONTEXT.md` D-16), and the workflow file states that in a comment so a future reader does not "complete" the required set with it. |
| T-05-40 | Repudiation | a scheduled workflow that has never actually run | medium | mitigate | Task 3 triggers one run with `gh workflow run` and the acceptance criterion asserts a completed run exists. |
| T-05-41 | Tampering | a Windows-only regression escaping because CI is Linux-only | low | accept | D-12: Windows runners cannot run Linux containers, so neither migration axis could run there. Recorded honestly in the SUMMARY rather than hidden; the development machine is the Windows evidence. |
</threat_model>

<verification>
- `grep -v '^[[:space:]]*#' .github/workflows/pr-gate.yml | grep -c 'needs:'` prints 0.
- All six job names appear exactly once each in the stripped workflow.
- `git diff --stat HEAD -- scripts/db-migrate.ts scripts/env.ts` is empty.
- The restore drill workflow has one completed run.
- A real pull request shows six checks reporting; record each one's conclusion and duration in the
  SUMMARY.
- `pnpm test` still passes locally.
</verification>

<success_criteria>
Every pull request runs the analyzer, both migration history axes and the application test suite as
six independent named checks; a reader inspecting the workflow sees migrations run as their own
isolated pipeline step against a database shaped to satisfy the existing pin; and the restore drill
runs on a schedule rather than on someone remembering.
</success_criteria>

<artifacts_this_phase_produces>
Introduced by this plan:

- **New workflow file:** `.github/workflows/restore-drill.yml` — `schedule` (weekly) plus
  `workflow_dispatch`, runs `pnpm test:drill`, reports failures onto a single tracking issue,
  deliberately not a required check.
- **Completed workflow:** `.github/workflows/pr-gate.yml` now carries all six jobs.
- **New required-check names:** `tamper-checks`, `test`, `test-history`, `migrate`,
  `ruleset-config-check` (joining `analyze` from plan `05-03`).
- **New CI environment contract:** `RECIPE_DEV_DATABASE_URL` and `RECIPE_DEV_DB_PASSWORD` are set
  at job level to values satisfying the pinned development target; no repository secret is added.

Phase-wide published contract. **Under D-04 these six strings are ruleset configuration and are
therefore a published contract, not an internal detail** — renaming a job without updating
`.github/rulesets/main-protection.json` leaves its required check permanently unreported, which
blocks every pull request rather than passing it:

- **Required-check names / workflow job `name:` values:** `analyze`, `tamper-checks`, `test`,
  `test-history`, `migrate`, `ruleset-config-check`.
- **New workflow files:** `.github/workflows/pr-gate.yml`, `.github/workflows/restore-drill.yml`.
- **New ruleset payload:** `.github/rulesets/main-protection.json` (ruleset name `main-protection`).
- **New package exports:** `renderPrComment`, `renderAnalyzerFailureComment`, `PR_COMMENT_MARKER`,
  type `AnalyzedFile`.
- **New CI scripts (all):** `audit-history.ts`, `analyze-gate.ts`, `check-append-only.ts`,
  `check-schema-drift.ts`, `apply-ruleset.ts`, `check-ruleset-config.ts` — all under `scripts/ci/`.
- **New docs:** `docs/40-public-release-audit.md`, `docs/40-ci-gate-merge-attempt.md`.
- **New pnpm scripts:** none — CI-internal scripts are invoked as
  `pnpm exec tsx scripts/ci/<name>.ts`.
</artifacts_this_phase_produces>

<flagged_assumptions>
Surfaced, not silently dropped (spec-less probe fallback, §A):

- **CI-02 (unclassified edge probe)** — no shape category returned by the probe for "a BLOCKED
  classification fails the build"; covered by plan `05-03`'s exit-code mapping and its per-code
  unit tests.
- **CI-06 (unclassified edge probe)** — no shape category returned for "migrations run as their
  own pipeline step and never at application startup". This plan covers the first clause (the
  dedicated `migrate` job) and plan `05-07` covers the second (the boot-time guardrail); the row
  stays flagged because the probe produced no predicate, not because the requirement is uncovered.
</flagged_assumptions>

<output>
Create `.planning/phases/05-ci-pipeline-gate/05-06-SUMMARY.md` when done.
</output>
