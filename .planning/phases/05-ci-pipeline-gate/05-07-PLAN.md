---
phase: 05-ci-pipeline-gate
plan: 07
type: execute
wave: 6
depends_on: ["05-06"]
files_modified:
  - tests/guardrails.test.ts
autonomous: true
requirements: [CI-01, CI-06]

estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "No file in the application's own source can trigger a migration at boot: the guardrail suite fails if `apps/recipe-app/src/**` ever references the runner, the migration entry point, or a schema-sync command."
    - "A weakened rules file makes the analyzer refuse to start rather than emit a softer verdict, proven by behaviour against a synthetic rules file — not by reading `floor.ts`."
    - "Every context string in `.github/rulesets/main-protection.json` matches exactly one job `name:` in `.github/workflows/pr-gate.yml`, and no two jobs share a `name:` — so a required context can never be ambiguous or orphaned."
    - "The guardrail suite's source surface includes `.github/`, so a workflow file is covered by the same schema-sync ban, migrate-sub-command ban and direct-connection-variable-read ban that already cover `scripts/`, `tests/`, `packages/` and the application."
    - "Any PostgreSQL connection string appearing in a CI workflow file targets exactly the pinned development host, port and database — the allowlist that permits it also constrains it."
    - "Every new `scripts/ci/*.ts` module is covered by the no-target rule: none reads `process.argv`, stdin, or an interactive prompt library."
  artifacts:
    - path: "tests/guardrails.test.ts"
      provides: "D-11's two-property proof, D-15's boot-time migration ban, and the widened CI source surface"
      contains: "apps/recipe-app/src"
  key_links:
    - from: "tests/guardrails.test.ts"
      to: ".github/rulesets/main-protection.json"
      via: "the required-check context list is read from the committed payload and matched against the workflow's job names"
      pattern: "main-protection"
    - from: "tests/guardrails.test.ts"
      to: "packages/automation/src/classifier/floor.ts"
      via: "drives loadRules against a synthetic weakened rules file and asserts it throws"
      pattern: "RulesFileError"
  prohibitions:
    - statement: "MUST NOT allow a second, ungated path to schema state — the application's own startup, a workflow step, or any file in the source surface invoking a schema-sync or migrate sub-command directly."
      status: resolved
      verification: judgment
---

<objective>
Turn three properties this phase currently relies on into things a test fails on.

Purpose:
- **D-15** — `tests/guardrails.test.ts` already covers `01-CONTEXT.md` D-14's container-init-script
  mount, but nothing asserts that the application's own code never triggers a migration at boot,
  which is CI-06's second clause and `docs/decisions.md` D8's direct subject. This is a cheap test
  closing a real gap found while scouting, not a hypothetical one.
- **D-11** — the same-pull-request desync attack (an agent editing the rules file or the workflow
  file in the same pull request as the dangerous migration) is already closed structurally. This
  phase's job is to add a test proving both halves still hold and to state them in the record, not
  to build a third mechanism.
- **The CI source surface** — `.github/` did not exist when `sourceSurfaceFiles()` was written.
  A workflow file is now a place a second migrate path could appear, so it belongs inside the
  scan for the same reason `packages/` was added in Phase 3.

Output: `tests/guardrails.test.ts` extended with three new assertions in its existing
`describe("structural guardrails")` block.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ci-pipeline-gate/05-CONTEXT.md
@.planning/phases/05-ci-pipeline-gate/05-RESEARCH.md
@tests/guardrails.test.ts
@packages/automation/src/classifier/floor.ts
@docs/decisions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-15 — the application's own code never triggers a migration at boot</name>
  <read_first>
    - `tests/guardrails.test.ts` lines 1-160 — the file header, `ALLOWED_ROOT_FILES`,
      `sourceSurfaceFiles()`, the runtime-built-needle idiom, and the two narrow allowlists with
      their recorded reasons.
    - `tests/guardrails.test.ts` lines 278-300 — the existing D-14 container-init-script guardrail,
      the closest sibling to the new assertion and the shape to mirror.
    - `tests/guardrails.test.ts` lines 482-500 — the unconditional, zero-exemption migrate
      sub-command ban and its recorded reasoning ("a gate with a second door is not a gate").
    - `apps/recipe-app/src/db/client.ts` — what the application legitimately imports today: the
      shared env accessor and the Drizzle schema, and nothing from `packages/automation`'s runner.
    - `apps/recipe-app/src/app/layout.tsx` and `apps/recipe-app/src/app/page.tsx` — the real boot
      path, so the assertion's scope is derived from what actually runs rather than assumed.
    - `docs/decisions.md` § D8 — migrations never run at application startup; the decision this
      assertion mechanises.
    - `scripts/db-migrate.ts` lines 19-35 — the runner symbols whose presence in application source
      would be the violation: `runMigrations`, `ensureDrizzleLedger`, `enumerateMigrationFiles`,
      `RUNNER_CONNECTION_OPTIONS`.
  </read_first>
  <files>tests/guardrails.test.ts</files>
  <behavior>
    - The assertion enumerates every tracked file under `apps/recipe-app/src/` and every
      Next.js configuration/instrumentation file in `apps/recipe-app/`, and asserts none of them
      references the migration entry point, the runner's public symbols, or a schema-sync command.
    - The assertion is non-vacuous: it first asserts the enumerated file list is non-empty, so a
      renamed directory or a gitignore change produces a failure rather than a silent pass.
    - Comment-only lines are stripped before scanning, matching the file's existing idiom, so a
      comment describing the constraint cannot trip the check that enforces it.
    - Needles are built at runtime by concatenation, so this test file's own source never contains
      the literal expressions it searches for.
  </behavior>
  <action>
    Add one test to the existing `describe("structural guardrails", ...)` block in
    `tests/guardrails.test.ts`, named for D-15 and CI-06's second clause.

    Enumerate the application's own source surface from `git ls-files` — every tracked path under
    `apps/recipe-app/src/` plus `apps/recipe-app/next.config.ts` and any
    `apps/recipe-app/instrumentation*.ts` (Next.js's own startup-hook convention; enumerate by
    prefix so a file added later is covered by default rather than exempt by default). Assert that
    list is non-empty before scanning anything, following the precedent already set by the
    `sourceSurfaceFiles()` non-vacuity test at the top of the block.

    For each file, strip comment-only lines, then assert the content does not reference: the
    migration package script name (`db:migrate`), the runner's exported entry symbol
    (`runMigrations`), the ledger bootstrap (`ensureDrizzleLedger`), the migration enumerator
    (`enumerateMigrationFiles`), or the schema-sync sub-command the file already bans elsewhere.
    Build every needle at runtime by concatenation, matching the file's own convention.

    Exempt nothing. `apps/recipe-app/src/db/seed.ts` legitimately writes rows but does not migrate,
    and `client.ts` legitimately opens a pool through the shared env module — neither references any
    of the above, so an unconditional ban costs nothing today. That is the same argument the
    existing migrate-sub-command ban records for choosing the zero-exemption shape over an
    allowlist. If a file does trip it, that is a finding to investigate, not a reason to add an
    exemption.

    The failure message must name the offending file, the offending symbol, and state that
    migrations reach the database only through the pipeline-invoked runner, never through the
    application's own startup path.
  </action>
  <acceptance_criteria>
    - `tests/guardrails.test.ts` contains a new test whose name references D-15 and boot-time
      migrations.
    - That test asserts the enumerated application-source file list length is greater than zero
      before making any content assertion.
    - The test builds its needles at runtime rather than as literals, so `tests/guardrails.test.ts`
      itself does not fail the assertion it adds.
    - `pnpm exec vitest run tests/guardrails.test.ts` passes.
    - Temporarily adding a reference to the runner entry symbol in a scratch file under
      `apps/recipe-app/src/` makes the test fail; the executor performs this falsification, records
      the observed failure message in the SUMMARY, and removes the scratch file. The suite is green
      at commit time.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/guardrails.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed"</fails_when>
    <automated>pnpm test</automated>
    <fails_when>non-zero exit, or the summary line reports any failed test file</fails_when>
  </verify>
  <done>CI-06's second clause is mechanically enforced: the application's own source cannot reference the migration path, and the check is proven non-vacuous by a real falsification the executor performed and recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-11 — prove both halves of the same-pull-request desync closure</name>
  <read_first>
    - `packages/automation/src/classifier/floor.ts` (whole file) — `assertFloorNotWeakened` and
      `assertUnmatchedDefaultsToReview`, the two load-time self-checks, and exactly which call path
      invokes them.
    - `packages/automation/src/classifier/classify.ts` — `loadRules`, the function under test; the
      assertion drives real behaviour rather than grepping for the self-check's name.
    - `packages/automation/src/classifier/rules-schema.ts` — the schema a synthetic weakened rules
      file must still satisfy, so the test proves the *floor* rejects it rather than the schema.
    - `packages/automation/src/types.ts` lines 198-206 — `RulesFileError`, the error the weakened
      file must produce.
    - `.github/rulesets/main-protection.json` — the six committed contexts.
    - `.github/workflows/pr-gate.yml` — the six job `name:` values those contexts must match.
    - `.planning/phases/05-ci-pipeline-gate/05-RESEARCH.md` § Pitfall 2 and § Summary — a required
      status check that never reports blocks the merge rather than silently passing; that property,
      not a rule about editing workflow files, is what closes the delete-the-job attack.
    - `.planning/research/SUMMARY.md` line ~59 — the desync attack named in the original research.
  </read_first>
  <files>tests/guardrails.test.ts</files>
  <behavior>
    - Loading a synthetic rules file whose content weakens a floor operation below BLOCKED throws
      `RulesFileError`, and the message names the offending value.
    - Loading a synthetic rules file whose default for an unmatched statement is weaker than
      REVIEW REQUIRED throws `RulesFileError`.
    - The committed ruleset payload's context set is exactly equal to the set of job `name:` values
      in `pr-gate.yml` — neither a context without a job nor a job that is required but unlisted.
    - No two jobs in `pr-gate.yml` share a `name:` value.
  </behavior>
  <action>
    Add two tests to the existing `describe("structural guardrails", ...)` block.

    **First half — the rules floor refuses to load when weakened.** Drive `loadRules` (imported
    from `packages/automation`) against synthetic rules content held in memory, never against the
    real committed rules file and never by writing to disk. Construct one variant that weakens a
    floor operation below BLOCKED and one whose unmatched-statement default is weaker than REVIEW
    REQUIRED, and assert each throws `RulesFileError` with a message naming the offending value.
    This proves the property by behaviour: an agent that edits the rules file in the same pull
    request as a dangerous migration makes the analyzer refuse to start, and the `analyze` job's
    exit-40 mapping (plan `05-03`) fails the check rather than emitting a softer verdict. Assert
    behaviour, not the presence of a function name in a source file — a grep for
    `assertFloorNotWeakened` would still pass if the call were deleted from the load path.

    **Second half — the required-check names live in the ruleset, not the workflow.** Read
    `.github/rulesets/main-protection.json` and extract its required-status-check context strings.
    Read `.github/workflows/pr-gate.yml` and extract every job `name:` value (strip comment lines
    first; match the `name:` key at job indentation, not step-level `name:` keys). Assert the two
    sets are exactly equal in both directions, and assert the job-name list contains no duplicate.
    This is the adjacency property: two contexts that are equal collide, and a context adjacent to
    no job is orphaned; the equality assertion catches both.

    Add a comment above the second test recording, in one sentence, the property that makes this
    the closure rather than a rule about editing workflow files: deleting or renaming a job means
    its named required check never reports, and a required check that never reports blocks the
    merge rather than passing it — confirmed against GitHub's own troubleshooting documentation
    (05-RESEARCH) and observed live in plan `05-08`. State honestly that this test cannot prove
    GitHub's behaviour; it proves the names agree, which is the half this repository owns.
  </action>
  <acceptance_criteria>
    - `tests/guardrails.test.ts` contains a test asserting `loadRules` throws `RulesFileError` for
      a synthetic weakened-floor rules file, driven from an in-memory value with no file written.
    - `tests/guardrails.test.ts` contains a test asserting exact set equality between the ruleset
      payload's contexts and the workflow's job names, failing in both directions.
    - That test also asserts the job-name list has no duplicate entry.
    - The set-equality test asserts the extracted context list is non-empty before comparing, so a
      broken extraction cannot pass by comparing two empty sets.
    - `pnpm exec vitest run tests/guardrails.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/guardrails.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed"</fails_when>
    <automated>pnpm test</automated>
    <fails_when>non-zero exit, or the summary line reports any failed test file</fails_when>
  </verify>
  <done>Both halves of D-11 are tested: a weakened rules file refuses to load, and the required-check names in the ruleset and the job names in the workflow are proven to be the same set with no duplicates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Widen the guardrail source surface to cover CI configuration</name>
  <read_first>
    - `tests/guardrails.test.ts` lines 20-90 — `ALLOWED_ROOT_FILES`,
      `FIXTURE_FILES_WITH_CONNECTION_STRINGS`, `FIXTURE_FILES_READING_DEV_CONNECTION_VARIABLE`,
      `CONNECTION_STRING_SCHEME_PREFIX`, and `sourceSurfaceFiles()` with the Phase 3 comment
      recording that `packages/` was an oversight rather than a deliberate exclusion.
    - `tests/guardrails.test.ts` lines 154-182 — the non-vacuity test that asserts each claimed
      root actually yields files.
    - `tests/guardrails.test.ts` lines 40-56 — the `KNOWN_PACKAGE_BOUNDARY_ESCAPES` inventory and
      its recorded reasoning: an inventory that *constrains* beats an allowlist that merely
      *exempts*, because an allowlist exempts files while an inventory records exactly what is
      permitted and fails on anything more.
    - `docs/decisions.md` § D25 — the same reasoning stated as a decision record; the shape this
      task's new allowlist follows.
    - `scripts/env.ts` lines 55-70 — the three pinned target components the CI connection string
      must be asserted to equal.
    - `.github/workflows/pr-gate.yml` — the file the widened surface will now scan.
  </read_first>
  <files>tests/guardrails.test.ts</files>
  <behavior>
    - `sourceSurfaceFiles()` returns at least one file under `.github/`.
    - The non-vacuity test's claimed-roots list includes `.github/` so the widening cannot be
      vacuous.
    - The connection-string-prefix assertion still passes: the one workflow file carrying a CI
      connection string is permitted by a named, constrained allowlist.
    - That allowlist entry does more than exempt: for each permitted file, the test parses every
      connection string it contains and asserts the host is in the pinned allowlist, the port is
      the pinned port, and the database name is the pinned name. A CI connection string pointing
      anywhere else fails.
    - The new `scripts/ci/*.ts` modules are covered by the existing no-target rule: none references
      `process.argv`, stdin, or an interactive prompt library.
  </behavior>
  <action>
    Extend `sourceSurfaceFiles()` with `if (file.startsWith(".github/")) return true;`, carrying a
    comment in the same voice as the existing `packages/` comment: `.github/` did not exist when
    this enumeration was written, and a workflow file is now a place a second ungated path to
    schema state could appear, so the schema-sync ban, the migrate-sub-command ban and the
    direct-connection-variable-read ban should cover it exactly as they cover `scripts/`.

    Add `.github/` to the claimed-roots array in the non-vacuity test, so a widening that silently
    yields nothing fails rather than passing.

    Introduce a third narrow, named constant — `CI_WORKFLOW_FILES_WITH_EPHEMERAL_DSN` — listing
    exactly `.github/workflows/pr-gate.yml`, and exclude those files from the
    connection-string-prefix assertion. Follow D25's shape rather than the plain-allowlist shape:
    add a **second** assertion that iterates the same constant and, for every connection string in
    each listed file, parses it and asserts the host is a member of `DEV_DATABASE_HOST_ALLOWLIST`,
    the port equals `EXPECTED_DEV_DATABASE_PORT`, and the database name equals
    `EXPECTED_DEV_DATABASE_NAME` — all three imported from `scripts/env.ts`, never re-typed. The
    entry therefore does not exempt the file from scrutiny; it swaps a ban for a constraint, and
    it makes D-14's "shape the CI database to fit the pin, never loosen the pin to fit CI" a tested
    property rather than a hope. Assert the constant is non-empty and that every listed file exists,
    so a renamed workflow fails loudly instead of quietly disabling the constraint.

    Finally, extend the existing `commandScripts` array in the no-target test with every
    `scripts/ci/*.ts` module this phase added: `audit-history.ts`, `analyze-gate.ts`,
    `check-append-only.ts`, `check-schema-drift.ts`, `apply-ruleset.ts`, `check-ruleset-config.ts`.
    Every one of them takes its configuration from the Actions environment, so extending the rule
    costs nothing today and makes "a command that accepts an argument is the command that later
    accepts a target" hold for the CI surface too. Derive the list from a `git ls-files scripts/ci`
    enumeration rather than a hand-typed array, so a seventh CI script added later is covered by
    default rather than exempt by default.
  </action>
  <acceptance_criteria>
    - `sourceSurfaceFiles()` returns at least one path beginning with `.github/`, asserted by the
      existing non-vacuity test with `.github/` added to its roots list.
    - `tests/guardrails.test.ts` defines `CI_WORKFLOW_FILES_WITH_EPHEMERAL_DSN` and asserts it is
      non-empty and that every listed path exists in `git ls-files`.
    - A test asserts every connection string in every listed workflow file resolves to a host in
      `DEV_DATABASE_HOST_ALLOWLIST`, port `EXPECTED_DEV_DATABASE_PORT`, and database
      `EXPECTED_DEV_DATABASE_NAME`, with all three imported from `scripts/env.ts`.
    - The no-target test's script list is derived from `git ls-files scripts/ci` rather than
      hand-typed, and the test asserts that derived list is non-empty.
    - `pnpm exec vitest run tests/guardrails.test.ts` passes.
    - `pnpm test` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run tests/guardrails.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed"</fails_when>
    <automated>pnpm test</automated>
    <fails_when>non-zero exit, or the summary line reports any failed test file</fails_when>
    <automated>pnpm exec tsx scripts/ci/check-schema-drift.ts</automated>
    <fails_when>non-zero exit — the guardrail changes must not have disturbed the migrations directory</fails_when>
  </verify>
  <done>CI configuration is inside the same source surface as the rest of the repository, and the one connection string a workflow legitimately carries is constrained to the pinned development target rather than merely excused.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application runtime → database schema | The application must consume an already-migrated schema and must never be able to change it. |
| pull-request author → the analyzer's own rules | Rules and workflow files are editable in the same pull request as the migration they would classify. |
| CI configuration → the repository's structural constraints | Workflow files were previously outside every guardrail this repository enforces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-42 | Elevation of Privilege | the application booting itself into a migration | high | mitigate | Task 1's unconditional ban across every tracked application source and Next.js startup-hook file, proven non-vacuous by a real falsification the executor performs and records. |
| T-05-43 | Tampering | a weakened rules file shipped alongside a dangerous migration | critical | mitigate | Task 2 drives `loadRules` against synthetic weakened content and asserts `RulesFileError`; the analyze job maps that to a failing check. Behaviour is asserted, not the presence of a symbol. |
| T-05-44 | Tampering | a required job renamed or deleted alongside a dangerous migration | high | mitigate | Task 2's exact set equality between the committed ruleset contexts and the workflow job names, in both directions, with a non-empty precondition and a no-duplicates assertion. The GitHub half — a never-reporting required check blocks the merge — is observed live in plan `05-08`, not assumed here. |
| T-05-45 | Tampering | a second ungated path to schema state introduced inside a workflow file | high | mitigate | Task 3 widens the guardrail source surface to `.github/`, bringing the existing schema-sync and migrate-sub-command bans over CI configuration; the non-vacuity roots list is extended so the widening cannot pass having examined nothing. |
| T-05-46 | Information Disclosure | a CI connection string pointing at something other than the ephemeral job container | high | mitigate | Task 3's allowlist constrains rather than exempts: every connection string in a permitted workflow file is parsed and asserted against the three pinned target components imported from `scripts/env.ts`. |
| T-05-47 | Tampering | a CI script later growing an argument that becomes a database target | medium | mitigate | Task 3 extends the no-target rule to every `scripts/ci/*.ts` module, derived from `git ls-files` so a new script is covered by default. |
| T-05-48 | Repudiation | a guardrail that passes having scanned nothing | high | mitigate | Every new assertion asserts its enumerated input is non-empty first, following the precedent the file's own `sourceSurfaceFiles()` non-vacuity test set in Phase 3. |
</threat_model>

<verification>
- `pnpm exec vitest run tests/guardrails.test.ts` passes.
- `pnpm test` passes.
- The SUMMARY records the observed failure message from Task 1's falsification, proving the
  boot-time ban is not vacuous.
- `git status --porcelain` is clean — no scratch file from the falsification remains.
</verification>

<success_criteria>
Three properties this phase previously relied on are now things a test fails on: the application
cannot migrate itself at boot, a weakened rules file refuses to load, and the required-check names
in the ruleset are provably the same set as the job names in the workflow — with CI configuration
now inside the same source surface as the rest of the repository.
</success_criteria>

<artifacts_this_phase_produces>
Introduced by this plan:

- **New guardrail constant:** `CI_WORKFLOW_FILES_WITH_EPHEMERAL_DSN` in `tests/guardrails.test.ts`
  — a constraining inventory in D25's shape, not an exemption list.
- **Widened source surface:** `sourceSurfaceFiles()` now enumerates `.github/`, so the schema-sync
  ban, the migrate-sub-command ban and the direct-connection-variable-read ban cover CI
  configuration.
- **Extended no-target coverage:** every `scripts/ci/*.ts` module, derived from `git ls-files`.
- **Three new guardrail assertions:** D-15's boot-time migration ban, D-11's rules-floor refusal,
  and the ruleset-context/job-name set equality.

Phase-wide published contract. **Under D-04 these six strings are ruleset configuration and are
therefore a published contract, not an internal detail** — this plan's set-equality assertion is
what keeps the two sides of that contract from drifting:

- **Required-check names / workflow job `name:` values:** `analyze`, `tamper-checks`, `test`,
  `test-history`, `migrate`, `ruleset-config-check`.
- **New workflow files:** `.github/workflows/pr-gate.yml`, `.github/workflows/restore-drill.yml`.
- **New ruleset payload:** `.github/rulesets/main-protection.json` (ruleset name `main-protection`).
- **New package exports:** `renderPrComment`, `renderAnalyzerFailureComment`, `PR_COMMENT_MARKER`,
  type `AnalyzedFile`; plus types `Journal` and `JournalEntry`, promoted from unexported
  declarations in `adapter/drizzle-migrations.ts` and re-exported type-only from the barrel by
  plan `05-04` so the append-only comparator shares one definition of a journal entry.
- **New CI scripts (all):** `audit-history.ts`, `analyze-gate.ts`, `check-append-only.ts`,
  `check-schema-drift.ts`, `apply-ruleset.ts`, `check-ruleset-config.ts` — all under `scripts/ci/`.
- **New docs:** `docs/40-public-release-audit.md`, `docs/40-ci-gate-merge-attempt.md`.
- **New pnpm scripts:** none — CI-internal scripts are invoked as
  `pnpm exec tsx scripts/ci/<name>.ts`.
</artifacts_this_phase_produces>

<flagged_assumptions>
Surfaced, not silently dropped (spec-less probe fallback, §A):

- **CI-02 (unclassified edge probe)** — no shape category returned; covered by plan `05-03`.
- **CI-06 (unclassified edge probe)** — no shape category returned. This plan covers the second
  clause (nothing at application startup migrates) and plan `05-06` covers the first (the isolated
  `migrate` job); the row stays flagged because the probe produced no predicate to resolve.

Additionally recorded honestly: Task 2's set-equality test proves that this repository's two sides
agree. It cannot prove GitHub's own behaviour that a never-reporting required check blocks a merge
— that is observed live in plan `05-08` and recorded as a backstop truth there, not asserted here.
</flagged_assumptions>

<output>
Create `.planning/phases/05-ci-pipeline-gate/05-07-SUMMARY.md` when done.
</output>
