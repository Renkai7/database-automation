---
phase: 04-migration-runner-history-tests
plan: 07
type: execute
wave: 6
depends_on: ["04-06"]
files_modified:
  - packages/automation/test/corpus/app-shaped/generated-drop-ingredients-table.sql
  - packages/automation/test/corpus/manifest.json
  - tests/history/blocked-replay.test.ts
  - docs/30-migration-runner.md
  - docs/migration-history-status.json
  - docs/decisions.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RUN-07, APP-02]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A real recipe-app schema change that lands BLOCKED — dropping the `ingredients` table — was edited into `schema.ts`, generated by `drizzle-kit generate`, run through `pnpm db:migrate`, and genuinely refused; the migration and its journal entry were then reverted, leaving the committed history unchanged."
    - "The refusal is replayed permanently by a committed test against a real database, so it never has to be taken on trust again."
    - "The exact SQL `drizzle-kit generate` produced for the drop is committed as a corpus fixture with its observed verdict, so its classification is regression-tested rather than remembered."
    - "The recipe application starts successfully against the schema produced by applying the full migration history through the runner, proven by `tests/smoke.test.ts` run unchanged after a full `pnpm db:reset`."
    - "No BLOCKED migration remains in the committed history or journal — `pnpm db:reset` replays that history on every rebuild and RUN-05 asserts on it."
    - "`docs/30-migration-runner.md` records what was actually run and what the runner actually printed for all three verdicts, including anything surprising."
    - "Every Phase 4 requirement's traceability row states its real status; nothing is marked complete that has not been verified."
  artifacts:
    - tests/history/blocked-replay.test.ts
    - packages/automation/test/corpus/app-shaped/generated-drop-ingredients-table.sql
    - docs/30-migration-runner.md
  key_links:
    - "`drizzle-kit generate` output → the committed corpus fixture → `tests/history/blocked-replay.test.ts`: the refusal is proven against the bytes drizzle actually produced, not against a hand-written approximation."
    - "`pnpm db:reset` → the runner applies the full history → `tests/smoke.test.ts` unchanged: RUN-07's proof reuses the asset `01-CONTEXT.md` D-08 built for exactly this."
    - "`packages/automation/test/corpus/manifest.json` → `corpus.test.ts`: every real migration and every app-shaped fixture keeps its recorded verdict under regression test."
  prohibitions:
    - "A BLOCKED migration must never remain in the committed migrations directory or the committed journal."
    - "`tests/smoke.test.ts` must not be modified, extended, or have assertions added to it."
    - "The reverted schema edit must leave no residue: no schema field, no `.sql` file, no journal entry, no snapshot."
    - "No expand-and-contract change may be made — APP-03 is reserved for Phase 7."
    - "No requirement checkbox may be ticked on the strength of implementation alone; verification is a separate step."
    - "Production's PostgreSQL major version must stay recorded as UNKNOWN, not assumed."
    - "No credential may be written into any documentation file."
    - "No approval or override mechanism for REVIEW REQUIRED may be built; that remains Phase 5 and Phase 7 work."
---

<objective>
Spend the third reserved churn row: make a real recipe-app schema change that the runner must
refuse, watch it be refused, revert it, and leave behind a committed test that replays the
refusal permanently. Then prove RUN-07 — the application boots against the schema the runner
produces — and close the phase's documentation and traceability.

Purpose: the remaining third of criterion 5, plus RUN-07, plus D-33's record. A BLOCKED
migration cannot be left in committed history because `pnpm db:reset` replays that history on
every rebuild and RUN-05 asserts on it, so the demonstration is necessarily transient and the
committed replay test is what makes it permanent.
Output: the refusal demonstration, `tests/history/blocked-replay.test.ts`, the completed
`docs/30-migration-runner.md`, and honest traceability rows.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-migration-runner-history-tests/04-CONTEXT.md
@.planning/phases/04-migration-runner-history-tests/04-PATTERNS.md
@.planning/phases/04-migration-runner-history-tests/04-06-SUMMARY.md
@CLAUDE.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

Symbols and files this plan creates (new — exclude from drift verification):

| Kind | Name / path |
|------|-------------|
| file | `packages/automation/test/corpus/app-shaped/generated-drop-ingredients-table.sql` |
| file | `tests/history/blocked-replay.test.ts` |
| manifest entry | one new `app-shaped` group entry in `packages/automation/test/corpus/manifest.json` |
| document sections | the BLOCKED demonstration and the surprises section of `docs/30-migration-runner.md` |
| decision entries | `docs/decisions.md`: the runner owns execution; no second migrate path; the runner-owned table and its bootstrap; recovery reports but never repairs; two test harnesses |

## Flagged planner assumptions (unresolved edge-coverage rows)

The deterministic edge probe classified this row `unclassified`; it stays **unresolved** and is
surfaced here rather than auto-resolved or dropped:

- **RUN-07 — unclassified.** Assumption: "the application starts successfully against the
  resulting schema" is satisfied by `tests/smoke.test.ts` run unchanged against the real pinned
  development container after a full-history `pnpm db:reset` (D-26). That test boots the app and
  asserts a real HTTP response for a seeded recipe; it deliberately does not assert the APP-02
  columns, because `01-CONTEXT.md` D-08 built it as a stable reusable asset and D-26 declined to
  extend it. If RUN-07 is read as requiring the boot to also assert the new columns, this plan
  does not satisfy that reading — a new test, not an edit to that one, is where that would go.

<tasks>

<task type="auto">
  <name>Task 1: The BLOCKED change — generated for real, refused, reverted, and replayed forever after</name>
  <files>packages/automation/test/corpus/app-shaped/generated-drop-ingredients-table.sql, packages/automation/test/corpus/manifest.json, tests/history/blocked-replay.test.ts</files>
  <read_first>
    - `apps/recipe-app/src/db/schema.ts` — as left by plan 04-06; the `ingredients` table and its relations, both of which the temporary edit removes.
    - `packages/automation/test/corpus/app-shaped/drop-ingredients-table.sql` and its manifest entry — the Phase 3 mirror (`BLOCKED`, rule `drop-table`) written so this real change could be made already knowing the verdict.
    - `tests/history/tamper-then-refuse.test.ts` — written in plan 04-03; its temp-migrations-directory mechanism and container helpers are reused here directly.
    - `packages/automation/src/runner/exit-codes.ts` — `RUNNER_EXIT_CODES.REFUSED_BLOCKED`.
    - `apps/recipe-app/drizzle/meta/_journal.json` — its current state, which must be byte-identical again after the revert.
  </read_first>
  <action>
**Make the change for real.** Temporarily remove the `ingredients` table and its
`ingredientsRelations` from `apps/recipe-app/src/db/schema.ts`, and remove `ingredients` from
`recipesRelations`'s `many(...)` block so the file still type-checks. Run `pnpm db:generate` and
read the generated `.sql` — it should be a `DROP TABLE` on `ingredients`, possibly with a
constraint drop alongside it. Capture its exact bytes.

**Watch it be refused.** Run `pnpm db:migrate`. Expected: the runner refuses with verdict
`BLOCKED`, rule id `drop-table`, exit code `RUNNER_EXIT_CODES.REFUSED_BLOCKED` (21), nothing
applied, no new `drizzle.__drizzle_migrations` row, and a `runner.migration_runs` row with state
`refused`. Capture the complete output verbatim for Task 3. Confirm afterwards that the
`ingredients` table still exists in the live development database and still holds its seeded
rows. If the runner did anything other than refuse, stop — that is a finding that outranks the
demonstration.

**Revert completely.** Restore `apps/recipe-app/src/db/schema.ts`,
`apps/recipe-app/drizzle/meta/_journal.json` and the snapshot directory to their committed state,
and delete the newly generated `.sql` file and its snapshot. `git status --porcelain apps/recipe-app`
must be empty afterwards — no schema field, no migration file, no journal entry, no snapshot may
survive. A BLOCKED migration left in committed history would be replayed by every `pnpm db:reset`
and asserted on by RUN-05.

**Commit the captured SQL as a corpus fixture.**
`packages/automation/test/corpus/app-shaped/generated-drop-ingredients-table.sql` holds the exact
bytes `drizzle-kit generate` produced — not the Phase 3 hand-written mirror, which may quote
identifiers differently. Add its manifest entry in the `app-shaped` group with the observed
verdict and rule ids and a `why` recording that this is the real generated output of a real
schema deletion, kept so the refusal is regression-tested against the bytes drizzle actually
produces. Leave the existing `drop-ingredients-table.sql` entry untouched — it is the Phase 3
prediction, and keeping both makes the prediction-versus-reality comparison permanent.

**`tests/history/blocked-replay.test.ts`** — the committed replay (D-32). Copy the committed
`apps/recipe-app/drizzle/` directory into an `mkdtemp` directory; add
`generated-drop-ingredients-table.sql`'s bytes as a new `.sql` file there with a matching
`meta/_journal.json` entry whose `idx` and `when` follow the newest committed entry. Start a
fresh `postgres:17` container, `ensureDrizzleLedger`, `ensureRunnerTable`, then call
`runMigrations` with `enumerateMigrationFiles(tempDir, tempJournalPath)`. Assert: the call
rejects carrying `RUNNER_EXIT_CODES.REFUSED_BLOCKED`; the `runner.migration_runs` row for it has
state `refused`, verdict `BLOCKED`, and a `findings` array containing rule id `drop-table`; the
`ingredients` table **still exists** in the container, because every earlier migration applied
and the drop did not; `drizzle.__drizzle_migrations` holds exactly the committed journal's entry
count and no more; and running the same temp directory a second time refuses identically.
Remove the temp directory and stop the container in `finally` blocks. Timeout 180000.
  </action>
  <verify>
    <automated>pnpm exec vitest run --config vitest.history.config.ts tests/history/blocked-replay.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or the assertion that the ingredients table still exists reports it as absent</fails_when>
    <automated>git status --porcelain apps/recipe-app</automated>
    <fails_when>the command prints any line — the temporary BLOCKED change left residue in the schema, the migrations directory, the journal, or a snapshot</fails_when>
    <automated>pnpm exec vitest run packages/automation/test/corpus.test.ts</automated>
    <fails_when>non-zero exit, or the corpus harness reports any fixture whose actual verdict differs from its manifest entry</fails_when>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain apps/recipe-app` is empty after the demonstration.
    - `apps/recipe-app/drizzle/meta/_journal.json` is byte-identical to its state at the start of this task.
    - `packages/automation/test/corpus/app-shaped/generated-drop-ingredients-table.sql` exists, contains the bytes `drizzle-kit generate` actually produced, and has exactly one manifest entry with verdict `BLOCKED`.
    - The pre-existing manifest entry for `packages/automation/test/corpus/app-shaped/drop-ingredients-table.sql` is byte-identical to its previous value.
    - `tests/history/blocked-replay.test.ts` asserts the `ingredients` table still exists after the refusal, and that a second run refuses identically.
    - The runner's captured output for the refused migration names verdict `BLOCKED`, rule `drop-table`, and exit code 21.
  </acceptance_criteria>
  <done>Criterion 5's BLOCKED third is done for real — generated, refused, reverted — and the refusal is replayed permanently by a committed test against a real database.</done>
</task>

<task type="auto">
  <name>Task 2: RUN-07 — the application boots against the schema the runner produced</name>
  <files>docs/migration-history-status.json</files>
  <read_first>
    - `tests/smoke.test.ts` — read it, do not modify it. Note how it spawns and terminates the app (including the `taskkill /T /F` path on win32) and what it asserts.
    - `scripts/db-reset.ts` — the full teardown-and-rebuild order the runner now sits inside, and the independent `assertMigrationHistoryApplied` post-check that follows it.
    - `.planning/phases/04-migration-runner-history-tests/04-CONTEXT.md` D-26 — why this test is reused verbatim and why extending it was rejected.
  </read_first>
  <action>
Run `pnpm db:reset` — a full teardown of the container and its volume, a fresh container, the
complete migration history applied through the runner (including the three migrations plan 04-06
added), the independent post-check, and the seed. Then run
`pnpm exec vitest run tests/smoke.test.ts` against that result, with the file **unchanged**.

Record in the summary: that `db:reset` reported the runner's per-migration verdicts on the way
through (including `0001_busy_thunderbolt.sql`'s REVIEW REQUIRED, which every rebuild exercises
for free — D-07), that the post-check passed, and that the smoke test passed.

Then run `pnpm test:history` once more so the committed record reflects the suite as it now
stands, including `tests/history/blocked-replay.test.ts`, and commit the updated
`docs/migration-history-status.json` with the newly measured runtime recorded in the summary.

Do not add assertions to `tests/smoke.test.ts`, do not add a wrapper around it, and do not add a
new test in this task — if a future phase wants app-level assertions on the APP-02 columns, they
belong in a new file, not in this one.
  </action>
  <verify>
    <automated>pnpm db:reset</automated>
    <fails_when>non-zero exit, or the output contains "FAILED at step", or the completion line is absent</fails_when>
    <automated>pnpm exec vitest run tests/smoke.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>pnpm test:history</automated>
    <fails_when>non-zero exit, or docs/migration-history-status.json does not contain "outcome": "PASS" after the command completes</fails_when>
    <automated>git status --porcelain tests/smoke.test.ts</automated>
    <fails_when>the command prints any line — tests/smoke.test.ts was modified, which D-26 forbids</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm db:reset` completes with the runner applying every committed migration and the independent post-check passing.
    - `pnpm exec vitest run tests/smoke.test.ts` passes against the resulting database, with the file unmodified.
    - `git status --porcelain tests/smoke.test.ts` is empty.
    - `docs/migration-history-status.json` is committed with `outcome: "PASS"` and a `lastRunAt` later than plan 04-06's.
  </acceptance_criteria>
  <done>RUN-07 is proven: the recipe application starts successfully against the schema produced by applying the full migration history through the runner, using the smoke test built for exactly this and reused unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: The runner record, the decisions, and honest traceability</name>
  <files>docs/30-migration-runner.md, docs/decisions.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `docs/30-migration-runner.md` — as left by plan 04-06, including its marked placeholder for the BLOCKED demonstration.
    - `docs/decisions.md` — the entry format, the explicit-status convention, the existing D-numbering, and D16's record that production's PostgreSQL major version is UNKNOWN.
    - `.planning/REQUIREMENTS.md` — the RUN-01…RUN-08 and APP-02 checkbox lines and the traceability table rows, and the footer note's own convention.
    - `docs/20-restore-runbook.md` — the "written from what actually happened" precedent.
    - The verbatim runner output captured in plan 04-06 Tasks 1-2 and in this plan's Task 1.
  </read_first>
  <action>
**`docs/30-migration-runner.md`** — replace the placeholder with the BLOCKED demonstration:
the schema edit made, the SQL `drizzle-kit generate` produced (verbatim), the command run, the
runner's complete refusal output including every rule id and rationale, the exit code, and the
revert. Then complete the document:
- a short "what actually surprised us" section covering every divergence between prediction and
  reality across all three demonstrations, and anything the seed, the app, or the history suite
  had to change to accommodate them;
- a "recovering from a partial failure" section pointing at `pnpm db:migrate:recover` and stating
  plainly that it reports and resolves the marker but repairs nothing;
- an "what this does not do" section: no approval or override path for REVIEW REQUIRED (Phase 5
  and Phase 7), no remote or production database, no audit log yet, and production's PostgreSQL
  major version still UNKNOWN.
Never paste a connection string, a role name's password, or any credential.

**`docs/decisions.md`** — add these entries, each with an explicit status, in the file's existing
format (plan 04-02 already added the floor-widening and timeout entries; do not duplicate them):
1. The migration runner executes SQL itself through `pg`, classifying the exact in-memory buffer
   it is about to execute; wrapping drizzle's own programmatic migrator was rejected because it
   re-reads the files and wraps each file in a transaction. Status ACCEPTED.
2. Drizzle-kit's own migrate sub-command is structurally unreachable from this repository, with
   no renamed escape hatch; a future pull request restoring one is a safety-relevant change.
   Status ACCEPTED.
3. The runner owns a separate `runner.migration_runs` table in the same database, bootstrapped
   imperatively with `CREATE ... IF NOT EXISTS` rather than by a committed Drizzle migration, with
   the three reasons recorded (the chicken-and-egg of recording the run that creates it; it is
   runner infrastructure rather than application schema; RUN-05's schema assertions must stay a
   statement about the application's schema). `drizzle.__drizzle_migrations` stays byte-compatible
   and untouched. Status ACCEPTED.
4. Recovery is report-and-resolve, never repair: `pnpm db:migrate:recover` names the migration,
   the statement and every INVALID index it finds, then resolves the marker; it never edits the
   journal and never drops anything. Status ACCEPTED.
5. RUN-05/RUN-06 run against Testcontainers while RUN-07's app boot runs against the real pinned
   development container, because the app's connection is pinned and loosening that pin was
   refused. Status ACCEPTED.
Re-state, do not quietly drop, that production's PostgreSQL major version remains UNKNOWN.

**`.planning/REQUIREMENTS.md`** — update the traceability rows for `RUN-01 … RUN-08` and `APP-02`
from `Pending` to `Implemented — awaiting phase verification`, naming the plan that implemented
each where the table's format allows. Do **not** tick the checkboxes in the requirements list:
implementation is not verification, and this repository already has a recorded instance
(plan 01-08) of deliberately declining to claim completion on implementation alone. Refresh the
footer note to say what changed and when, in the voice the existing note uses.
  </action>
  <verify>
    <automated>pnpm test</automated>
    <fails_when>non-zero exit, or any line matching "FAIL " in the output</fails_when>
    <automated>grep -c 'UNKNOWN' docs/decisions.md</automated>
    <fails_when>the printed count is 0 — the record that production's PostgreSQL major version is unverified was dropped rather than carried forward</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/30-migration-runner.md` contains a verbatim runner report for each of the three verdicts (SAFE, REVIEW REQUIRED, BLOCKED) and contains no remaining placeholder marker.
    - `docs/30-migration-runner.md` contains no PostgreSQL connection-string scheme prefix and no password.
    - `docs/decisions.md` gained five new entries, each with an explicit status, and none duplicates an entry plan 04-02 added.
    - `.planning/REQUIREMENTS.md`'s traceability rows for `RUN-01 … RUN-08` and `APP-02` no longer read `Pending`.
    - No checkbox in `.planning/REQUIREMENTS.md`'s requirement list was ticked by this task.
  </acceptance_criteria>
  <done>The phase's record is written from what actually happened, its decisions are recorded with explicit statuses, and its traceability states the true status of every requirement without over-claiming.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a deliberately destructive schema edit → the runner | The one demonstration where the gate is asked to do its actual job. |
| committed migrations directory → every future `pnpm db:reset` | Anything left behind here is replayed on every rebuild forever. |
| `docs/` → future readers | Documentation is a deliverable in this project; a false claim here is a real defect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-48 | Tampering | a BLOCKED migration is accidentally left in committed history and replayed by every rebuild | critical | mitigate | `git status --porcelain apps/recipe-app` must be empty and the journal byte-identical, both as automated verify commands and as acceptance criteria. |
| T-04-49 | Elevation of Privilege | the destructive demonstration actually executes against the development database | high | mitigate | The refusal is asserted against the live database (`ingredients` still present with its seeded rows) and replayed against a throwaway container, never trusted from the exit code alone. |
| T-04-50 | Repudiation | the refusal is claimed in documentation but not reproducible | high | mitigate | `tests/history/blocked-replay.test.ts` replays it permanently against the exact bytes drizzle generated, committed as a corpus fixture with its verdict pinned. |
| T-04-51 | Tampering | a stable reusable test asset is quietly extended, so later phases inherit something different from what they were promised | medium | mitigate | `git status --porcelain tests/smoke.test.ts` is an automated verify command; D-26's rationale is restated in the task. |
| T-04-52 | Repudiation | a requirement is marked complete on the strength of implementation alone | medium | mitigate | The traceability rows read "Implemented — awaiting phase verification" and no checkbox is ticked; this follows the recorded plan 01-08 precedent. |
| T-04-53 | Information Disclosure | a credential is written into `docs/30-migration-runner.md` while pasting verbatim runner output | high | mitigate | The runner's own output is produced through `safeErrorMessage` and carries no connection string by construction; an acceptance criterion additionally asserts the file contains no connection-string prefix and no password. |
| T-04-54 | Repudiation | an unverified fact (production's PostgreSQL major version) is quietly recorded as known | medium | mitigate | An automated verify command asserts `UNKNOWN` still appears in `docs/decisions.md`, and the task restates the requirement to carry it forward. |
| T-04-55 | Tampering | npm/pip/cargo dependency substitution | low | accept | This plan installs zero new packages. |
</threat_model>

<verification>
- `pnpm test` and `pnpm test:history` are both green.
- `pnpm db:reset` followed by `pnpm exec vitest run tests/smoke.test.ts` passes.
- `git status --porcelain apps/recipe-app` and `git status --porcelain tests/smoke.test.ts` are both empty.
- `docs/30-migration-runner.md` carries verbatim runner output for all three verdicts.
- `.planning/REQUIREMENTS.md`'s Phase 4 traceability rows state implementation status honestly, with no checkbox ticked.
</verification>

<success_criteria>
- Criterion 5 is complete: one real schema change of each kind has been run through the runner and produced the expected outcome, with the BLOCKED case refused for real and replayed permanently.
- RUN-07 is proven with the smoke test reused verbatim.
- The phase's decisions and record exist as committed documents, and every unverified thing is still marked UNKNOWN.
</success_criteria>

<output>
Create `.planning/phases/04-migration-runner-history-tests/04-07-SUMMARY.md` when done.
</output>
