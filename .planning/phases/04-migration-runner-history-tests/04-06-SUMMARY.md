---
phase: 04-migration-runner-history-tests
plan: 06
subsystem: database
tags: [postgresql, drizzle-orm, drizzle-kit, migration-runner, corpus, squawk]

# Dependency graph
requires:
  - phase: 04-migration-runner-history-tests
    provides: "04-01's runner core (runMigrations, classify-then-execute) and pnpm db:migrate entry point; 04-04's transaction policy (wrap-or-refuse) and pinned timeouts, exercised live for the first time by a real, generated migration; 04-05's marker lifecycle, which every applied migration in this plan travels through cleanly; 04-03's tests/history/support.ts Testcontainers harness, whose EXPECTED_FULL_HISTORY_COLUMNS this plan updates"
provides:
  - "apps/recipe-app/src/db/schema.ts: recipes.notes (nullable) and steps.timerLabel (.notNull().default('')) -- two of 01-CONTEXT.md D-11's three reserved recipe-app churn rows, spent for real"
  - "Three new committed, applied Drizzle migrations (0002_oval_maelstrom.sql, 0003_backfill_steps_timer_label.sql, 0004_redundant_apocalypse.sql) generated by drizzle-kit and applied through the real pnpm db:migrate runner -- never hand-edited, never applied via a direct schema-sync command"
  - "packages/automation/test/corpus/manifest.json's real-migration group widened from two entries to five, pinning the observed verdicts of both real changes so their classification is regression-tested going forward"
  - "docs/30-migration-runner.md -- the first half of D-33's phase-output record: what the runner is, its pinned timeouts, what it does with each verdict, and the verbatim runner output for both real changes; a placeholder section awaits plan 04-07's BLOCKED demonstration"
affects: [04-07-blocked-demonstration (spends the third and final reserved churn row, and fills this plan's docs/30-migration-runner.md placeholder), 05-ci-safety-gate (the runner's REVIEW_REQUIRED-proceeds-locally behavior this plan exercised live is exactly what Phase 5's CI gate sits in front of), 07-production-runner (the corpus manifest and squawk-comparison report this plan widened are the same artifacts a second consuming project's Phase 7 extraction would carry forward)]

# Actuals (#2632)
actuals:
  tokens: 14645
  tasks: 3
  commits: 4
  plan_head_before: 1b19621

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A hand-authored custom migration (drizzle-kit generate --custom) sits between two generated migrations in the same journal, proving the runner classifies and applies whatever bytes are on disk regardless of how the file was produced -- the backfill's own verdict (REVIEW_REQUIRED, no rule matched) was not predicted by any committed corpus fixture in advance; it was observed live and only then pinned into the manifest, following 03-05-SUMMARY.md's own 'derive corpus fixtures by execution, not by reading rules.json prose' discipline."
    - "A committed comparison/analysis document (docs/30-squawk-comparison.md) that must never be silently regenerated is updated by hand, appending new rows and disagreement write-ups at the file's end rather than reinserting them at manifest position -- the same precedent 03-07/04-02's own 'Audit note' sections already established, now extended a third time."

key-files:
  created:
    - apps/recipe-app/drizzle/0002_oval_maelstrom.sql
    - apps/recipe-app/drizzle/0003_backfill_steps_timer_label.sql
    - apps/recipe-app/drizzle/0004_redundant_apocalypse.sql
    - apps/recipe-app/drizzle/meta/0002_snapshot.json
    - apps/recipe-app/drizzle/meta/0003_snapshot.json
    - apps/recipe-app/drizzle/meta/0004_snapshot.json
    - docs/30-migration-runner.md
  modified:
    - apps/recipe-app/src/db/schema.ts
    - apps/recipe-app/src/db/seed.ts
    - apps/recipe-app/drizzle/meta/_journal.json
    - packages/automation/test/corpus/manifest.json
    - packages/automation/test/adapter.test.ts
    - packages/automation/test/corpus.test.ts
    - tests/dev-database-baseline.test.ts
    - tests/history/support.ts
    - tests/history/existing-db-newest-only.test.ts
    - docs/30-squawk-comparison.md
    - docs/migration-history-status.json

key-decisions:
  - "The plan's Task 2 read_first list named tests/history/empty-db-full-history.test.ts as the file to update with the new expected column shapes; the actual EXPECTED_FULL_HISTORY_COLUMNS constant lives in tests/history/support.ts (imported by that test), so support.ts was edited directly rather than the test file itself -- functionally identical requirement, different location than the plan's own files_modified list named."
  - "tests/history/existing-db-newest-only.test.ts's RUN-06 assertion that the newest migration adds at least one new table no longer holds now that the real newest migrations (0002/0003/0004) alter existing tables' columns rather than creating tables -- widened to a full schema-shape (tables + columns) inequality check, preserving the same 'the staged database was genuinely missing something' property the original assertion existed to prove, without assuming the something is a table."
  - "docs/30-squawk-comparison.md was updated by hand against a disposable squawk probe run over exactly the three new files, never through the full generator (packages/automation/scripts/squawk-comparison.ts), which would have overwritten every hand-filled disagreement analysis already committed -- following the CR-02 and Phase-4-plan-02 'Audit note' precedent already established in that same file."

patterns-established:
  - "A test asserting a specific structural consequence of 'the newest migration' (here: adds a new table) must assert on the general property it actually needs (schema shape changed), not a specific mechanism (a table was added) that happens to hold only for the migrations that existed when the test was written -- D-25's own reasoning (both test halves always derive from the real committed journal, never a fixture) extends naturally to this case."

requirements-completed: [APP-02]

coverage:
  - id: D1
    description: "A real recipe-app schema change that lands SAFE (nullable notes column on recipes) was generated by drizzle-kit generate and applied through pnpm db:migrate, and the runner reported SAFE for it"
    requirement: APP-02
    verification:
      - kind: integration
        ref: "pnpm db:migrate live run against the pinned development database -- runner JSON report: 0002_oval_maelstrom verdict SAFE, rule add-column-nullable-no-default, state applied, exit 0"
        status: pass
      - kind: unit
        ref: "packages/automation/test/corpus.test.ts#corpus fixtures match their manifest row exactly -- apps/recipe-app/drizzle/0002_oval_maelstrom.sql"
        status: pass
    human_judgment: false
  - id: D2
    description: "A real recipe-app schema change that lands REVIEW REQUIRED (SET NOT NULL on steps.timer_label, preceded by a backfill) was generated and applied through pnpm db:migrate, and the runner printed every rule id and every rationale before proceeding"
    requirement: APP-02
    verification:
      - kind: integration
        ref: "pnpm db:migrate live run -- runner stdout prints the complete findings for both 0003_backfill_steps_timer_label (REVIEW_REQUIRED, no rule matched) and 0004_redundant_apocalypse (REVIEW_REQUIRED, statement 0 no rule matched, statement 1 set-not-null), then exits 0"
        status: pass
      - kind: unit
        ref: "packages/automation/test/corpus.test.ts#corpus fixtures match their manifest row exactly -- apps/recipe-app/drizzle/0003_backfill_steps_timer_label.sql, apps/recipe-app/drizzle/0004_redundant_apocalypse.sql"
        status: pass
    human_judgment: false
  - id: D3
    description: "The backfill's scoped UPDATE ... WHERE timer_label IS NULL demonstrates the update-without-where floor rule from the passing side -- a scoped update proceeds where an unscoped one would be BLOCKED"
    requirement: APP-02
    verification:
      - kind: integration
        ref: "0003_backfill_steps_timer_label.sql live-classified REVIEW_REQUIRED (not BLOCKED) via pnpm db:analyze; packages/automation/test/corpus/blocked/update-without-where.sql (the unscoped counterpart) remains classified BLOCKED, unchanged by this plan"
        status: pass
    human_judgment: false
  - id: D4
    description: "The live development database schema agrees with apps/recipe-app/src/db/schema.ts after both changes, brought into agreement through the governed runner and never through a direct schema-sync command"
    requirement: APP-02
    verification:
      - kind: integration
        ref: "pnpm db:query against the live database after both migrations: recipes.notes present with is_nullable YES; steps.timer_label is_nullable NO, column_default ''::text, zero NULL rows"
        status: pass
    human_judgment: false
  - id: D5
    description: "pnpm db:reset still completes end to end after both changes: the full history applies through the runner, the seed succeeds, and the independent post-check passes"
    requirement: APP-02
    verification:
      - kind: integration
        ref: "pnpm db:reset live run (full teardown, docker compose up, runner-applied history, seed, assertMigrationHistoryApplied post-check) -- completed with no manual intervention"
        status: pass
    human_judgment: false
  - id: D6
    description: "The two new real migrations are recorded in the analyzer's corpus manifest with their observed verdicts, so their classification is regression-tested from now on rather than remembered"
    requirement: APP-02
    verification:
      - kind: unit
        ref: "packages/automation/test/corpus.test.ts (80 tests, including the widened real-migration group check) and packages/automation/test/anlz-07.test.ts"
        status: pass
    human_judgment: false
  - id: D7
    description: "apps/recipe-app/drizzle/0001_busy_thunderbolt.sql is untouched -- it is a live REVIEW REQUIRED specimen exercised by every db:reset, not a defect"
    requirement: APP-02
    verification:
      - kind: automated_ui
        ref: "git log --oneline -- apps/recipe-app/drizzle/0000_bumpy_khan.sql apps/recipe-app/drizzle/0001_busy_thunderbolt.sql shows no commit newer than the commits that originally created them"
        status: pass
    human_judgment: false

# Metrics
duration: 24min
completed: 2026-09-08
status: complete
---

# Phase 4 Plan 6: Real SAFE and REVIEW REQUIRED Schema Changes Summary

**Spent two of `01-CONTEXT.md` D-11's three reserved recipe-app churn rows for real -- a nullable `recipes.notes` column (SAFE) and a backfilled `steps.timer_label` `SET NOT NULL` (REVIEW REQUIRED) -- each generated by `drizzle-kit generate` and applied through the real `pnpm db:migrate` runner, with the runner's actual output captured verbatim and both verdicts matching the Phase 3 corpus predictions exactly.**

## Performance

- **Duration:** 24 min (22:28 session start -> 22:52 completion, UTC)
- **Started:** 2026-09-08T22:28:23Z (approx., STATE.md's own prior session timestamp)
- **Completed:** 2026-09-08T22:52:42Z
- **Tasks:** 3 (all `type="auto"`, no checkpoints)
- **Files modified:** 18 (7 created, 11 modified)

## Accomplishments

- `apps/recipe-app/src/db/schema.ts`: `recipes.notes` (nullable `text`, no default) and
  `steps.timerLabel` (`.notNull().default("")`) -- two of the three reserved churn rows named in
  `01-CONTEXT.md` D-11, spent for real. The `tags` table alternative and the expand-and-contract
  row (Phase 7) remain unspent.
- Three new committed, applied migrations: `0002_oval_maelstrom.sql` (generated, `ADD COLUMN
  notes text`), `0003_backfill_steps_timer_label.sql` (hand-authored via `drizzle-kit generate
  --custom`, a single scoped `UPDATE`), `0004_redundant_apocalypse.sql` (generated, `SET DEFAULT
  ''` then `SET NOT NULL`). Every generated file's SQL was confirmed to be exactly what was
  expected before applying it -- none was hand-edited.
- `pnpm db:migrate` applied all three through the real runner: `0002` classified SAFE
  (`add-column-nullable-no-default`); `0003` and `0004` both classified REVIEW_REQUIRED, with the
  runner printing the complete findings for each (including `0004`'s two-statement finding list)
  before proceeding locally and exiting 0. Both real verdicts matched the Phase 3 corpus
  predictions (`recipes-add-notes-column.sql`, `steps-timer-label-set-not-null.sql`) with zero
  divergence to report.
- `pnpm db:reset` rebuilt and reseeded cleanly on top of both changes with no manual
  intervention: Drizzle's own insert genuinely omits an unset column rather than sending an
  explicit `NULL`, so the two timer-less seed rows picked up the column's `''` default
  automatically. Live-confirmed against the seeded rows.
- `packages/automation/test/corpus/manifest.json`'s `real-migration` group widened from two
  entries to five, pinning all three new migrations' observed verdicts as regression tests; the
  two pre-existing entries (`0000_bumpy_khan.sql`, `0001_busy_thunderbolt.sql`) are
  byte-identical to their pre-task values.
- `docs/30-migration-runner.md` created: the first half of D-33's phase-output record, with the
  runner's complete verbatim output for both real changes and a clearly marked placeholder for
  plan 04-07's BLOCKED demonstration. Contains no connection string or credential.

## Task Commits

Each task was committed atomically:

1. **Task 1: The SAFE change -- notes column on recipes** - `6e4c768` (feat)
2. **Task 2: The REVIEW REQUIRED change -- backfill then SET NOT NULL on steps.timer_label** - `b30a072` (feat)
3. **Task 3: The new migrations join the corpus, and the first half of the runner record** - `37e4b26` (docs)

**Plan metadata:** committed alongside this SUMMARY (see below).

## Files Created/Modified

- `apps/recipe-app/src/db/schema.ts` - `recipes.notes`, `steps.timerLabel` widened to `NOT NULL DEFAULT ''`
- `apps/recipe-app/src/db/seed.ts` - comment updated (no longer asserts SQL NULL for timer-less steps)
- `apps/recipe-app/drizzle/0002_oval_maelstrom.sql` / `0003_backfill_steps_timer_label.sql` / `0004_redundant_apocalypse.sql` - the three new real migrations
- `apps/recipe-app/drizzle/meta/_journal.json` - three new journal entries
- `packages/automation/test/corpus/manifest.json` - three new `real-migration` entries
- `docs/30-migration-runner.md` - D-33's first-half record (new)
- `docs/migration-history-status.json` - fresh recorded PASS
- `packages/automation/test/adapter.test.ts` - migration-file-count expectation widened 2 -> 5
- `packages/automation/test/corpus.test.ts` - `REAL_MIGRATION_PATHS` widened to all five real migrations
- `tests/dev-database-baseline.test.ts` - `recipes` column baseline updated 9 -> 10 (`notes` deliberately spent)
- `tests/history/support.ts` - `EXPECTED_FULL_HISTORY_COLUMNS` updated (`recipes.notes`, `steps.timer_label` not nullable)
- `tests/history/existing-db-newest-only.test.ts` - RUN-06's "newest migration adds a table" assertion widened to a full schema-shape comparison
- `docs/30-squawk-comparison.md` - three rows appended by hand, row counts and executive-summary tallies updated, audit-provenance note added

## Decisions Made

- The plan's Task 2 read_first named `tests/history/empty-db-full-history.test.ts` as the file
  carrying the expected column shapes; the actual `EXPECTED_FULL_HISTORY_COLUMNS` constant lives
  in `tests/history/support.ts` (imported by that test) -- edited there directly, since that is
  where the requirement's real target lives.
- `docs/30-squawk-comparison.md` was updated by hand against a disposable squawk probe over
  exactly the three new files, never through the full generator script, which would have
  overwritten every hand-filled disagreement analysis already committed to that file -- following
  the same "Audit note" precedent CR-02 and Phase 4 plan 02 already established there.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Pre-existing hardcoded test expectations broken by the real schema change**
- **Found during:** Task 1 and Task 2 (`pnpm test`)
- **Issue:** `packages/automation/test/adapter.test.ts` hardcoded `enumerateMigrationFiles()` to
  return exactly 2 entries; `tests/dev-database-baseline.test.ts` hardcoded `public.recipes`'s
  column set to its Phase 1 nine-column baseline. Both assumptions were correct before this
  plan and broken by its own intentional real schema change.
- **Fix:** Widened `adapter.test.ts`'s expectation to the actual five committed migrations (in
  journal order); updated `dev-database-baseline.test.ts`'s baseline to ten columns, including
  `notes`, with a comment recording this as a deliberate spend of D-30, not a silent relaxation.
- **Files modified:** `packages/automation/test/adapter.test.ts`, `tests/dev-database-baseline.test.ts`
- **Verification:** `pnpm test` green (436/436) after Task 1; green again (436/436, then widened
  again) after Task 2.
- **Committed in:** `6e4c768` (Task 1), `b30a072` (Task 2)

**2. [Rule 1 - Bug] RUN-06's "newest migration adds a new table" assumption no longer held**
- **Found during:** Task 3 (`pnpm test:history`)
- **Issue:** `tests/history/existing-db-newest-only.test.ts` asserted the newest migration in the
  committed journal adds at least one new table -- true when the newest migration was
  `0001_busy_thunderbolt.sql` (creates `ingredients`/`steps`), false now that the real newest
  migrations (`0002`/`0003`/`0004`) alter existing tables' columns instead.
- **Fix:** Widened the assertion from a table-list comparison to a full schema-shape (tables +
  columns) inequality check, preserving the same "the staged database was genuinely missing
  something the newest migration adds" property without assuming the something is a table.
- **Files modified:** `tests/history/existing-db-newest-only.test.ts`
- **Verification:** `pnpm test:history` green (5 files, 13 tests) after the fix.
- **Committed in:** `37e4b26` (Task 3)

**3. [Rule 1 - Bug] `docs/30-squawk-comparison.md`'s structural-integrity test broke when the corpus manifest widened**
- **Found during:** Task 3 (`pnpm test`)
- **Issue:** `packages/automation/test/corpus.test.ts` hardcoded `REAL_MIGRATION_PATHS` to exactly
  the two pre-existing real migrations, and `squawk-comparison-report.test.ts` asserts every
  corpus manifest entry appears in the committed squawk-comparison report -- both broke the
  moment three new `real-migration` entries were added to the manifest.
- **Fix:** Widened `REAL_MIGRATION_PATHS` to all five real migrations. Ran squawk directly
  against exactly the three new files (a disposable probe script, discarded afterward, never
  the full report generator, which would have destroyed every hand-filled disagreement analysis
  already committed) and updated `docs/30-squawk-comparison.md` by hand: three appended
  comparison-table rows, updated row counts and executive-summary tallies, two new disagreement
  write-ups (both different-by-design), and an audit-provenance note recording what changed and
  why -- following the CR-02 and Phase 4 plan 02 precedent already established in that file.
- **Files modified:** `packages/automation/test/corpus.test.ts`, `docs/30-squawk-comparison.md`
- **Verification:** `pnpm exec vitest run packages/automation/test/squawk-comparison-report.test.ts`
  green (3/3); full `pnpm test` green (439/439).
- **Committed in:** `37e4b26` (Task 3)

---

**Total deviations:** 3 auto-fixed (all Rule 1 -- test/documentation corrections required by this
plan's own intentional, correctly-specified real schema changes; no scope creep, no production
runner behavior changed by any of them).
**Impact on plan:** All three necessary to keep `pnpm test` honestly green after real changes the
plan itself required. None weaken any acceptance criterion.

## Issues Encountered

None beyond the deviations above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- APP-02 is two-thirds complete: two of criterion 5's three demonstrations (SAFE, REVIEW
  REQUIRED) are done for real, through the runner, with the runner's actual output recorded in
  `docs/30-migration-runner.md`. Plan 04-07 spends the third reserved churn row (BLOCKED,
  generated-refused-reverted with a committed replay test) and fills this document's placeholder
  section.
- The `tags` table alternative and the expand-and-contract row (`01-CONTEXT.md` D-11's fourth
  row, Phase 7's) remain unspent.
- `pnpm test` (44 files, 439 tests) and `pnpm test:history` (5 files, 13 tests) both green;
  `docs/migration-history-status.json` carries a fresh `PASS` with a later `lastRunAt` than plan
  04-05's; `pnpm db:reset` completes end to end with no manual intervention;
  `pnpm db:analyze:migrations` exits 10 (worst committed verdict remains REVIEW_REQUIRED, from
  `0001_busy_thunderbolt.sql`, unchanged and untouched).
- No blockers for plan 04-07.

## Self-Check: PASSED

- All 7 files listed in `key-files.created` verified present on disk via `[ -f ]`.
- Commits `6e4c768` (Task 1), `b30a072` (Task 2), `37e4b26` (Task 3) all found via
  `git log --oneline --all`.
- `pnpm test` (44 files, 439 tests) green; `pnpm test:history` (5 files, 13 tests) green with a
  fresh recorded PASS; `pnpm db:reset` completes end to end; `pnpm db:analyze:migrations` exits 10.
- Plan-level `<verification>` block re-confirmed: `pnpm test` and `pnpm test:history` both green;
  `pnpm db:reset` completes (full history through the runner, seed succeeds, post-check passes);
  the live database's `recipes.notes` and `steps.timer_label` agree with `schema.ts`
  (`is_nullable YES`/`notes`, `is_nullable NO` + `column_default ''::text`/`timer_label`);
  `pnpm db:analyze:migrations` exits 10 and the corpus manifest matches every real migration's
  actual observed verdict.

---
*Phase: 04-migration-runner-history-tests*
*Completed: 2026-09-08*
