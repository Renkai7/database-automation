---
phase: 06-private-staging-connectivity
plan: 05
type: execute
wave: 4
depends_on: ["06-04"]
files_modified:
  - scripts/env.ts
  - scripts/env.test.ts
  - scripts/db-migrate-staging.ts
  - scripts/ci/wait-for-staging-port.ts
  - apps/recipe-app/src/db/client.ts
  - drizzle.config.ts
  - package.json
  - tests/guardrails.test.ts
autonomous: true
requirements: [CONN-04, CONN-05]

estimate:
  tokens: 42000
  raw_tokens: 84000
  tasks: 4
  confidence: medium

must_haves:
  truths:
    - "pnpm db:migrate:staging runs the identical runMigrations core the development entry point runs — the classifier, both history checks and the transaction policy are re-derived at execution time, not carried over from the pull-request gate."
    - "scripts/db-migrate-staging.ts accepts no argument, no flag and no ambient-variable inference: its destination is RECIPE_STAGING_MIGRATOR_DATABASE_URL and nothing else, so pnpm db:migrate on a developer machine can never reach staging even by accident."
    - "assertStagingDatabase refuses unless the connected server reports current_database() = recipe_staging, current_user = recipe_migrator, and the staging environment marker — all three read from the server, none parsed from the connection string."
    - "The staging pin asserts no literal host and no literal port, so swapping the connectivity mechanism changes no code outside the connectivity step (D-02)."
    - "scripts/ci/wait-for-staging-port.ts takes no argument: the local forwarded host and port are pinned source constants, so the wait helper cannot be repointed by whatever invoked it."
    - "The wait helper exits non-zero after a bounded timeout rather than continuing — a tunnel that did not come up is not a migration that succeeded."
    - "The staging entry point is the only new module on the staging-migrate path that imports pg or holds a connection; everything downstream takes an already-constructed client."
    - "Running the staging entry point twice over an unchanged migrations directory applies nothing the second time and exits with the applied code — the runner's existing ledger is what makes the repeat a no-op, and no new state is introduced here that could make a rerun destructive."
    - "The deployed recipe app can obtain a staging connection at all: apps/recipe-app/src/db/client.ts no longer has getDevDatabaseUrl() as its single possible destination, which as of plan 06-01 made a staging deployment structurally unable to connect regardless of configuration."
    - "The app selects its environment from one explicit, required variable — never by inferring which connection variable happens to be present — and an absent or unrecognised value is a hard failure, not a silent fall back to development."
    - "scripts/env.ts's import-time EnvSchema parse no longer hard-fails a staging deployment for want of RECIPE_DEV_DATABASE_URL, and still hard-fails a development machine that omits it."
    - "The staging app credential is recipe_app — a different role from the recipe_migrator credential the staging migrate entry point uses, so the running application cannot alter schema."
  artifacts:
    - scripts/db-migrate-staging.ts
    - scripts/ci/wait-for-staging-port.ts
  key_links:
    - "scripts/db-migrate-staging.ts -> packages/automation/src/index's runMigrations (the same core, so a classification change reaches staging automatically rather than through a second copy)"
    - "scripts/ci/wait-for-staging-port.ts's pinned local port -> the workflow's -L forward local port (a mismatch here makes the wait succeed against nothing, or fail against a healthy tunnel)"
    - "assertStagingDatabase -> readEnvironmentMarker from plan 06-04 (the staging half is read-only; it must never call an ensure)"
---

<objective>
Give staging its own pinned entry point over the identical migration runner core, and the one
mechanism-aware helper the connectivity step needs — without letting either learn what the
connectivity mechanism is.

Purpose: `01-CONTEXT.md` D-16 and `02-CONTEXT.md` D-06's "no command accepts a target" rule stays
literally true only if each command has exactly one possible destination fixed in source. A
`--env staging` flag would reintroduce a command that accepts a target in spirit, and inferring the
environment from whichever variable happens to be present would make the destination a property of
ambient environment rather than of the command. A separate thin sibling costs roughly forty lines
of deliberately divergent wiring and buys both.

Output: `scripts/db-migrate-staging.ts`, the dynamic half of the staging pin, and an argument-free
wait-for-port helper.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-private-staging-connectivity/06-CONTEXT.md
@.planning/phases/06-private-staging-connectivity/06-RESEARCH.md
@.planning/phases/06-private-staging-connectivity/06-PATTERNS.md
@.planning/phases/06-private-staging-connectivity/06-04-SUMMARY.md
@scripts/db-migrate.ts
@scripts/env.ts
@packages/automation/src/index.ts
@packages/automation/src/runner/exit-codes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The dynamic half of the staging pin — ask the server, three ways</name>
  <behavior>
    - `assertStagingDatabase` against a client reporting `current_database() = recipe_staging`, `current_user = recipe_migrator` and the staging marker resolves.
    - Against a client reporting `current_database() = recipe_dev` it rejects.
    - Against a client reporting the right database but `current_user = recipe_readonly` it rejects.
    - Against a client reporting the right database and user but a `development` marker it rejects.
    - Against a client reporting the right database and user but a null marker it rejects — and issues no seeding statement, asserted on the recorded statement sequence.
    - Against a client whose marker read throws it rejects.
    - No rejection message contains anything taken from the connection string.
    - `getStagingMigratorUrl` throws a value-free error naming its variable when the variable is unset, and refuses a URL whose database name is the development database name.
  </behavior>
  <read_first>
    - scripts/env.ts (as modified by plans 06-01 and 06-04 — `assertStagingConnectionShape`, `readEnvironmentMarker`, `STAGING_ENVIRONMENT_MARKER`, and the `getBackupDestination` opt-in-accessor precedent)
    - scripts/env.test.ts (the extended `stubClient` from plan 06-04, which already answers both statement shapes and records statement order)
    - 06-CONTEXT.md D-06 (why the dynamic half is the load-bearing one) and D-02 (why no literal host or port may be asserted)
    - 06-RESEARCH.md "Code Examples -> Staging pin, modeled on the existing dev pin (D-06)"
  </read_first>
  <files>scripts/env.ts, scripts/env.test.ts</files>
  <action>
In `scripts/env.ts` add `export const EXPECTED_STAGING_MIGRATOR_ROLE = "recipe_migrator";` and
`STAGING_MIGRATOR_DATABASE_URL_ENV_VAR = "RECIPE_STAGING_MIGRATOR_DATABASE_URL"`, alongside the
existing staging constants, with a comment stating that changing either is a safety-relevant source
diff.

Add `export async function assertStagingDatabase(client: QueryableClient): Promise<void>`. It
issues one query selecting `current_database()` and `current_user`, then calls
`readEnvironmentMarker`, and throws unless all three match `EXPECTED_STAGING_DATABASE_NAME`,
`EXPECTED_STAGING_MIGRATOR_ROLE` and `STAGING_ENVIRONMENT_MARKER`. It must never call
`ensureDevelopmentMarker` or any other ensure — an absent marker on staging is a refusal, not
something to repair — and the comment above it must say so. It must not catch the marker read's
error. The thrown message names the expected values and nothing observed from the connection
string. Note in a comment that this asserts strictly more than the development pin does and is
nonetheless indifferent to how the packet arrived, which is exactly what makes D-02's seam
possible: it is a fact the server reports, not a string someone typed.

Because `assertStagingDatabase` needs both a `current_user` column and the marker, and the existing
`QueryableClient` row shape is `{ name: string | null }`, either widen the interface once more to
`Array<Record<string, string | null>>` or issue two separate single-column queries. Prefer two
single-column queries if the widening ripples into the thirteen existing development call sites —
the goal is to add the staging assertion without loosening the development one. Record which you
chose and why in the SUMMARY.

Add `export function getStagingMigratorUrl(): string`, following the `getBackupDestination` opt-in
precedent exactly as `getStagingReadonlyUrl` does: read the variable, throw a value-free error
naming it when empty, call `assertStagingConnectionShape(value,
STAGING_MIGRATOR_DATABASE_URL_ENV_VAR)`, return it. Do NOT add it to `EnvSchema`.

Add one test per `<behavior>` bullet to `scripts/env.test.ts`, written RED first. The
"issues no seeding statement" bullet is asserted on the recorded statement sequence, not on the
thrown error.
  </action>
  <verify>
    <automated>pnpm exec vitest run scripts/env.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>pnpm exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>pnpm test</automated>
    <fails_when>non-zero exit, or any line beginning with "FAIL" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/env.ts` exports `assertStagingDatabase`, `getStagingMigratorUrl`, `EXPECTED_STAGING_MIGRATOR_ROLE` and `STAGING_MIGRATOR_DATABASE_URL_ENV_VAR`.
    - `assertStagingDatabase`'s body contains no call to any function whose name begins with `ensure`.
    - `scripts/env.ts` still exports no staging host constant and no staging port constant.
    - `scripts/env.test.ts` contains a case asserting that the staging path issues no seeding statement.
    - `pnpm exec tsc --noEmit` clean; the thirteen existing `assertDevelopmentDatabase` call sites compile unchanged.
    - `pnpm test` green.
  </acceptance_criteria>
  <done>The staging pin asks the server three independent questions and refuses on any mismatch, while asserting nothing about how the connection was routed.</done>
</task>

<task type="auto">
  <name>Task 2: The staging migrate entry point</name>
  <read_first>
    - scripts/db-migrate.ts (the whole file — this is the clone target D-05 names; note the header's "no command accepts a target" rationale, the `RUNNER_CONNECTION_OPTIONS` connect-time timeout application, the `MigrationRefusedError` exit-code branch, and the `run().then(...)` tail that avoids the Windows libuv crash)
    - packages/automation/src/index.ts (the barrel and exactly what it exports)
    - packages/automation/src/runner/exit-codes.ts (the `RUNNER_EXIT_CODES` contract this entry point returns)
    - 06-PATTERNS.md section "scripts/db-migrate-staging.ts (script, thin entry point)" (the concrete import and body shape to mirror)
    - tests/guardrails.test.ts lines 527-595 (the `commandScripts` array — the staging entry point takes no flag at all, unlike `scripts/db-migrate.ts`, so it belongs in that list)
  </read_first>
  <files>scripts/db-migrate-staging.ts, package.json, tests/guardrails.test.ts</files>
  <action>
Create `scripts/db-migrate-staging.ts` as a thin sibling of `scripts/db-migrate.ts` over the
identical runner core. Header comment, in the same voice as its sibling's: this is the only new
module on the staging-migrate path permitted to import `pg` or hold a connection (D-27/D-28);
it accepts no argument, no flag and no ambient-variable inference, so `pnpm db:migrate` on a
developer machine can never reach staging even by accident, because it is a different command
reading a different variable; it deliberately does NOT carry `db-migrate.ts`'s `--migrations-dir`
flag, since that flag exists only so a development test can drive the real command against a
temporary directory and staging has no such need; it never forces a synchronous process exit; and
it knows nothing about how the connection is routed — if the connectivity step ahead of it dies
mid-run, this command's own connection errors and the command exits non-zero, which is the
intended behaviour under `01-CONTEXT.md` D-20's hard-fail rule.

Body: `const MIGRATIONS_DIR = "apps/recipe-app/drizzle";` and the derived journal path as source
constants. `async function run(): Promise<number>` that calls `getStagingMigratorUrl()`, then
`assertStagingConnectionShape(url, STAGING_MIGRATOR_DATABASE_URL_ENV_VAR)` before opening anything,
then constructs `new Client({ connectionString: url, options: RUNNER_CONNECTION_OPTIONS })`, then
inside `try`/`finally`: `client.connect()`, `assertStagingDatabase(client)`, then
`assertTimeoutsInEffect(client)` returning `RUNNER_EXIT_CODES.REFUSED_TIMEOUTS_NOT_IN_EFFECT` on
failure, then `ensureDrizzleLedger(client)` and `ensureRunnerTable(client)` — recipe_migrator owns
both on staging, which is why the bootstrap granted it `CREATE` on the database — then
`enumerateMigrationFiles(MIGRATIONS_DIR, journalPath)`, `loadDefaultRules()`, and
`runMigrations(client, { migrations, rules })`. Print the complete report JSON — never a summary,
per `04-CONTEXT.md` D-05 — followed by one `[db:migrate:staging] <tag>: <state>[ (<verdict>)]` line
per entry. Return `RUNNER_EXIT_CODES.APPLIED` on success and `error.code` for a
`MigrationRefusedError`, rethrowing anything else. Close the client in `finally`. End with the
`run().then(code => { process.exitCode = code; }).catch(...)` tail using `safeErrorMessage`.

Do not write a literal PostgreSQL connection-string scheme prefix into this file.

Add `"db:migrate:staging": "tsx scripts/db-migrate-staging.ts"` to the root `package.json`.

In `tests/guardrails.test.ts`, add `"scripts/db-migrate-staging.ts"` to the `commandScripts` array,
and extend that test's comment to record why this entry point belongs in the list while
`scripts/db-migrate.ts` deliberately does not: the staging command carries no flag at all.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>pnpm exec vitest run tests/guardrails.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>pnpm exec tsx scripts/db-migrate-staging.ts</automated>
    <fails_when>exit code 0, or the error message does not name RECIPE_STAGING_MIGRATOR_DATABASE_URL — with the variable unset this command must refuse loudly rather than fall back to any other destination</fails_when>
    <automated>pnpm test</automated>
    <fails_when>non-zero exit, or any line beginning with "FAIL" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/db-migrate-staging.ts` contains no `process.argv`, no `process.stdin`, no `process.exit(`, no `--migrations-dir`, and no literal `postgres` scheme prefix.
    - `grep -c "getDevDatabaseUrl\|RECIPE_DEV_DATABASE_URL" scripts/db-migrate-staging.ts` reports 0 — the staging command cannot reach the development target either.
    - `grep -c "runMigrations" scripts/db-migrate-staging.ts` reports at least 1, and the file imports it from `packages/automation/src/index` rather than reimplementing anything.
    - With the migrator variable unset, `pnpm db:migrate:staging` exits non-zero naming that variable and opens no connection.
    - `tests/guardrails.test.ts`'s `commandScripts` array contains `scripts/db-migrate-staging.ts`.
    - `pnpm test` and `pnpm exec tsc --noEmit` green.
  </acceptance_criteria>
  <done>Staging has one pinned command with exactly one possible destination, running the identical gated runner core, and it refuses loudly when its variable is absent.</done>
</task>

<task type="auto">
  <name>Task 3: The argument-free wait-for-port helper</name>
  <read_first>
    - 06-RESEARCH.md "Pattern 3: The wait-for-port helper (no new dependency)" (the `node:net` poll loop — note that the research sketch reads `process.argv`, which this repository forbids for every module under `scripts/ci/`)
    - tests/guardrails.test.ts lines 527-595 (the ban is derived from `git ls-files scripts/ci`, so a new file there is covered automatically the moment it is committed)
    - scripts/log.ts (`safeErrorMessage`)
    - 06-RESEARCH.md "Package Legitimacy Audit" (why `wait-on`/`wait-port` are rejected in favour of ~15 lines)
  </read_first>
  <files>scripts/ci/wait-for-staging-port.ts</files>
  <action>
Create `scripts/ci/wait-for-staging-port.ts`. It polls a TCP endpoint until it accepts a connection
or a bounded deadline passes.

The research sketch for this helper takes host and port from `process.argv`. Do not do that: every
module under `scripts/ci/` is covered by this repository's unconditional no-argument ban, derived
at test time from `git ls-files scripts/ci`, and a command that accepts an argument is the command
that later accepts a target. Pin them instead as source constants —
`const LOCAL_FORWARD_HOST = "127.0.0.1";` and `const LOCAL_FORWARD_PORT = 15432;` — with a comment
stating that this local port must match the workflow's own forward, that both belong to the
connectivity step and to nothing downstream of it (D-02), and that changing either is a
safety-relevant source diff.

Implementation: a `tryConnect` returning a promise that resolves true on `connect` and false on
`error`, always ending the socket; a loop with a `DEADLINE_MS = 30000` budget and a 500 ms sleep
between attempts; a final throw naming the host, the port and the elapsed budget when the deadline
passes. Never `process.exit` — set `process.exitCode` once, in a `.catch` at module scope, rendering
the error with `safeErrorMessage`. Print one line on success reporting how long it waited, so a
workflow log shows whether the tunnel came up promptly or barely.

Add a header comment recording the discretionary decisions this file settles: the CI tunnel
lifecycle is inline workflow steps rather than a composite action (one fewer indirection for a
single consumer); the wait is a bounded poll rather than a fixed sleep, because a fixed sleep is
exactly the "remembered timing" this project's non-negotiables reject; and if the tunnel dies after
this helper succeeds, nothing here detects it — the migration's own connection error is what
surfaces it, and that is deliberate, because a watchdog here would be a second mechanism-aware
component downstream of the connectivity step.

Do not add a `pnpm` script for this helper: it is invoked by the workflow as
`pnpm exec tsx scripts/ci/wait-for-staging-port.ts`, matching how `scripts/ci/analyze-gate.ts` is
invoked (`05-03`'s recorded finding that `pnpm run`'s stdout banner corrupted a script's output).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>pnpm exec tsx scripts/ci/wait-for-staging-port.ts</automated>
    <fails_when>exit code 0 when no tunnel is running, or the process not having terminated within 45 seconds — with nothing listening on the pinned local port this must time out and exit non-zero</fails_when>
    <automated>pnpm exec vitest run tests/guardrails.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/wait-for-staging-port.ts` contains no `process.argv`, no `process.stdin`, no `process.exit(`, and no prompt library reference — the `scripts/ci` guardrail passes with the file present.
    - With the tunnel down, the helper exits non-zero within roughly 30 seconds and its message names the host, the port and the budget.
    - With `ssh -N staging-db` running, the helper exits 0 and prints how long it waited.
    - No new npm dependency was added: the helper's only external import is `node:net`, `package.json` is absent from this task's `files`, and `pnpm install --frozen-lockfile` still succeeds against the unchanged lockfile.
    - The header comment records all three discretionary decisions (inline steps, bounded poll not fixed sleep, no watchdog after success).
  </acceptance_criteria>
  <done>The connectivity step can prove "after me, the staging URL connects" with a bounded, argument-free check that adds no dependency and no second mechanism-aware component.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Give the deployed application a staging connection at all</name>
  <behavior>
    - With the environment selector set to `development`, the app's accessor returns the development URL and the existing loopback/`recipe_dev` pin still refuses everything it refused before.
    - With the selector set to `staging`, the accessor returns the staging app URL and refuses a URL whose database name is the development database name.
    - With the selector unset, the accessor throws a value-free error naming the selector variable and its two permitted values.
    - With the selector set to any third value (`production`, `prod`, `Staging`, empty string), it throws — the match is exact and case-sensitive, with no normalisation and no default.
    - With the selector set to `staging` but the staging app variable unset, it throws naming that variable, not the development one.
    - No thrown message interpolates anything taken from any supplied URL.
    - Importing `scripts/env.ts` with the selector set to `staging` and `RECIPE_DEV_DATABASE_URL` absent does not throw at module load.
    - Importing `scripts/env.ts` with the selector set to `development` and `RECIPE_DEV_DATABASE_URL` absent still throws at module load, naming that variable.
  </behavior>
  <read_first>
    - apps/recipe-app/src/db/client.ts (the whole file — three lines, and the comment explaining that the connection string is obtained only through the shared validated env module; that property must survive this change unchanged)
    - scripts/env.ts as modified by Task 1 above and by plans 06-01 and 06-04 — specifically `assertStagingConnectionShape`, `getStagingReadonlyUrl`, `getStagingMigratorUrl`, `readEnvironmentMarker`, and the `EnvSchema` module-load parse at lines 141-172 with the comment at 152-158 explaining why its failure path is wrapped
    - scripts/env.ts lines 35-45 — the bare-`DATABASE_URL` refusal (D-20). Coolify injects `DATABASE_URL` into linked services by default; that refusal is why the staging app must never be "linked" to the database resource in Coolify, and it stays exactly as it is.
    - drizzle.config.ts and apps/recipe-app/src/db/seed.ts (the other two module-load consumers of `EnvSchema` named in the comment at line 208 — establish what each actually needs before changing when that parse fires)
    - 06-CONTEXT.md D-12 (the capability split: which credential each actor gets) and D-06 (why inference from "whichever variable is present" is rejected)
  </read_first>
  <files>scripts/env.ts, scripts/env.test.ts, apps/recipe-app/src/db/client.ts, drizzle.config.ts</files>
  <action>
As of plan 06-01 the deployed application has exactly one possible destination —
`getDevDatabaseUrl()`, pinned to a loopback host, port 5432 and database `recipe_dev`. A staging
deployment cannot satisfy that pin under any configuration, so plan 06-08's requirement that "the
application can read and write the newly-migrated schema" is unreachable without this task. This
was a planning gap, discovered during Phase 6 setup; it is recorded here rather than worked around
at deploy time.

Add to `scripts/env.ts`, beside the existing staging constants:
`export const APP_ENVIRONMENT_ENV_VAR = "RECIPE_APP_ENVIRONMENT";`,
`export const EXPECTED_STAGING_APP_ROLE = "recipe_app";` and
`export const STAGING_APP_DATABASE_URL_ENV_VAR = "RECIPE_STAGING_APP_DATABASE_URL";`.

Add `export function getAppDatabaseUrl(): string`. It reads `process.env[APP_ENVIRONMENT_ENV_VAR]`
and switches on an exact, case-sensitive match: `"development"` returns `getDevDatabaseUrl()`;
`"staging"` returns a new `getStagingAppUrl()`; anything else — including absent, empty, and any
case variant — throws a value-free error naming `APP_ENVIRONMENT_ENV_VAR` and both permitted
values. Do NOT normalise case, do NOT trim, and do NOT default to `development`: a deployment that
forgot the selector must fail loudly rather than quietly connect somewhere. Record in a comment
that this is an explicit selector rather than inference over which connection variable happens to
be present, and why — D-06 rejects ambient-variable inference because it makes the destination a
property of environment rather than of the code.

Add `export function getStagingAppUrl(): string` following the `getStagingReadonlyUrl` precedent
exactly: read `STAGING_APP_DATABASE_URL_ENV_VAR`, throw a value-free error naming it when empty,
call `assertStagingConnectionShape(value, STAGING_APP_DATABASE_URL_ENV_VAR)`, return it. Do NOT
add it to `EnvSchema`.

**The `EnvSchema` module-load parse must become conditional.** It currently requires
`RECIPE_DEV_DATABASE_URL` at import time, which would hard-fail the staging app before any of the
above runs. Make the parse fire only when the selector is not `"staging"` — so a development
machine that omits the variable still fails exactly as it does today, while a staging deployment
does not. Update the comment block above the parse to say so. Verify against the other two
module-load consumers named at line 208: `drizzle.config.ts` and the seed script both target
development only, so both keep the eager parse. If either turns out to need a change, make the
smallest one that preserves its current development-side failure behaviour and record it in the
SUMMARY.

Change `apps/recipe-app/src/db/client.ts` to call `getAppDatabaseUrl()` instead of
`getDevDatabaseUrl()`. Change nothing else about that file: the pool construction, the schema
import, and the comment's claim that the connection string is obtained only through the shared
validated env module must all remain true. Update that comment to name the selector.

Do NOT add a server-reported marker assertion to the application's connection path in this task.
The app holds a long-lived pool and has no natural single point at which to run one, and
`assertStagingDatabase` from Task 1 is deliberately migrator-shaped (it expects `recipe_migrator`).
Record this as a known gap in the SUMMARY: the application's environment safety rests on the static
URL pin and the credential split, not on a server round trip. Plan 06-06's record already names the
same limitation for the synchronous drizzle-config path.

Add one test per `<behavior>` bullet to `scripts/env.test.ts`, written RED first. The two
module-load bullets need the env module imported fresh per case — use `vi.resetModules()` with
`process.env` mutated before a dynamic `import()`, matching however the three existing
missing/malformed-variable cases already do it; follow that existing shape rather than inventing a
second one.
  </action>
  <verify>
    <automated>pnpm exec vitest run scripts/env.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>pnpm exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>pnpm exec pnpm --filter recipe-app build</automated>
    <fails_when>non-zero exit — the app must still build with no database variable set at all, as it does today</fails_when>
    <automated>pnpm test</automated>
    <fails_when>non-zero exit, or any line beginning with "FAIL" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/env.ts` exports `getAppDatabaseUrl`, `getStagingAppUrl`, `APP_ENVIRONMENT_ENV_VAR`, `EXPECTED_STAGING_APP_ROLE` and `STAGING_APP_DATABASE_URL_ENV_VAR`.
    - `grep -c "getDevDatabaseUrl" apps/recipe-app/src/db/client.ts` reports 0.
    - `grep -c "toLowerCase\|toUpperCase\|trim()" scripts/env.ts` does not increase — the selector match is exact, with no normalisation.
    - `scripts/env.ts` still exports no staging host constant and no staging port constant, and the bare-`DATABASE_URL` refusal at lines 35-45 is unchanged.
    - `RECIPE_STAGING_APP_DATABASE_URL` does not appear inside the `EnvSchema` object literal.
    - With `RECIPE_APP_ENVIRONMENT=staging` and `RECIPE_DEV_DATABASE_URL` unset, importing `scripts/env.ts` does not throw; with the selector set to `development` and the same variable unset, it throws naming it.
    - `pnpm --filter recipe-app build` succeeds with no database variable set.
    - `pnpm test` and `pnpm exec tsc --noEmit` green.
  </acceptance_criteria>
  <done>A staging deployment of the recipe app can reach the staging database under the `recipe_app` credential, selected by an explicit required variable, without loosening the development pin or the bare-`DATABASE_URL` refusal by one line.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 06-05 share)

| Kind | Symbol / path | Notes |
|------|---------------|-------|
| File | `scripts/db-migrate-staging.ts` | Argument-free staging entry point over `runMigrations` |
| File | `scripts/ci/wait-for-staging-port.ts` | Pinned-constant TCP poll, no dependency |
| Export | `assertStagingDatabase(client)` (`scripts/env.ts`) | D-06 dynamic half, three server-reported facts |
| Export | `getStagingMigratorUrl()` (`scripts/env.ts`) | Opt-in accessor |
| Export | `EXPECTED_STAGING_MIGRATOR_ROLE` (`scripts/env.ts`) | `"recipe_migrator"` |
| Export | `STAGING_MIGRATOR_DATABASE_URL_ENV_VAR` (`scripts/env.ts`) | Variable name constant |
| pnpm script | `db:migrate:staging` | `tsx scripts/db-migrate-staging.ts` |
| Env var | `RECIPE_STAGING_MIGRATOR_DATABASE_URL` | CI only; never on the development machine (D-12) |
| Source constants | `LOCAL_FORWARD_HOST` / `LOCAL_FORWARD_PORT` = `127.0.0.1` / `15432` | Must match the workflow's `-L` forward |
| Export | `getAppDatabaseUrl()` (`scripts/env.ts`) | Explicit-selector accessor the deployed app uses |
| Export | `getStagingAppUrl()` (`scripts/env.ts`) | Opt-in accessor, `recipe_app` credential |
| Export | `APP_ENVIRONMENT_ENV_VAR` (`scripts/env.ts`) | `"RECIPE_APP_ENVIRONMENT"`, exact match, no default |
| Export | `EXPECTED_STAGING_APP_ROLE` (`scripts/env.ts`) | `"recipe_app"` |
| Env var | `RECIPE_APP_ENVIRONMENT` | `development` on the dev machine, `staging` in Coolify |
| Env var | `RECIPE_STAGING_APP_DATABASE_URL` | Coolify application only; the app's own credential |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner process -> staging Postgres | The first automated writer this project has ever pointed at a shared database. |
| The connectivity step -> everything after it | D-02's seam: only the step before this may know the mechanism. |
| `RECIPE_STAGING_MIGRATOR_DATABASE_URL` -> the entry point | The one variable that can carry a schema-changing credential. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-23 | Spoofing | staging entry point pointed at the wrong database | critical | mitigate | Static shape check plus three server-reported facts (`current_database`, `current_user`, the environment marker); no host or port assertion, so the check cannot be satisfied by routing alone. |
| T-06-24 | Elevation of Privilege | a target flag or ambient-variable inference creeping in later | high | mitigate | `scripts/db-migrate-staging.ts` is added to the guardrail suite's unconditional no-argument `commandScripts` list, so a future flag fails the suite. |
| T-06-25 | Tampering | classification verdict carried over from the pull-request gate | high | mitigate | The entry point calls `loadDefaultRules()` and `runMigrations` itself, re-deriving classification at execution time (`docs/decisions.md` D12); nothing accepts a pre-computed verdict token. |
| T-06-26 | Denial of Service | tunnel never comes up, or dies mid-migration | medium | mitigate | Bounded 30 s poll that exits non-zero; after it succeeds, the migration's own connection error surfaces a mid-run failure. No fixed sleep, no retry-forever, no warn-and-continue. |
| T-06-27 | Information Disclosure | migrator credential printed by the entry point or the helper | high | mitigate | `safeErrorMessage` only; no interpolation of any supplied value; no literal scheme prefix permitted in the source surface; the wait helper never sees a credential at all. |
| T-06-28 | Elevation of Privilege | development entry point reaching staging | high | mitigate | Separate commands reading separate variables; a grep asserts the staging entry point contains no reference to the development accessor or variable. |
| T-06-29 | Elevation of Privilege | the deployed application reaching the development database, or a developer's app run reaching staging | high | mitigate | One explicit selector variable with an exact, case-sensitive two-value match and no default; each branch reaches a different accessor validating a different pin. An absent or unrecognised selector throws rather than defaulting. |
| T-06-30 | Elevation of Privilege | the running application altering schema | high | mitigate | The app's staging credential is `recipe_app`, a distinct role from the `recipe_migrator` credential the migrate entry point uses (D-12's capability split, granted in plan 06-02). Verified in the real world by plan 06-08. |
| T-06-31 | Spoofing | the application connected to the wrong staging-shaped database | medium | accept | **Known gap, deliberately accepted this phase.** The app's environment safety rests on the static URL pin plus the credential split, not on a server round trip: it holds a long-lived pool with no natural single assertion point, and `assertStagingDatabase` is migrator-shaped. Plan 06-06's record names the same limitation for the synchronous drizzle-config path. |
| T-06-32 | Information Disclosure | Coolify injecting a bare `DATABASE_URL` into the linked application | high | mitigate | The existing D-20 refusal at `scripts/env.ts` lines 35-45 throws on the mere presence of that variable and is explicitly unchanged by this task; the staging application must not be "linked" to the database resource in Coolify. |
| T-06-SC | Tampering | package-manager installs | low | accept | No install task; the wait helper is hand-rolled specifically to avoid adding `wait-on`/`wait-port`, and the acceptance criteria assert the lockfile is unchanged. |
</threat_model>

<verification>
- `pnpm exec vitest run scripts/env.test.ts` green with all eight staging-pin behaviour cases.
- `pnpm db:migrate:staging` refuses loudly with its variable unset.
- `pnpm exec tsx scripts/ci/wait-for-staging-port.ts` times out non-zero with the tunnel down and exits 0 with it up.
- `apps/recipe-app/src/db/client.ts` no longer references `getDevDatabaseUrl`, and `pnpm --filter recipe-app build` still succeeds with no database variable set.
- Importing `scripts/env.ts` under `RECIPE_APP_ENVIRONMENT=staging` does not require `RECIPE_DEV_DATABASE_URL`; under `development` it still does.
- `pnpm test`, `pnpm exec vitest run tests/guardrails.test.ts` and `pnpm exec tsc --noEmit` green.
</verification>

<success_criteria>
Staging is reachable by exactly one command with exactly one possible destination, running the
identical gated runner core, guarded by three facts the server itself reports — and the only
mechanism-aware code in the phase is a bounded, argument-free port poll that adds no dependency.
</success_criteria>

<output>
Create `.planning/phases/06-private-staging-connectivity/06-05-SUMMARY.md` when done
</output>
